# A-G revision status (audited 2026-07-20, 36 items, 69 agents, adversarially verified)

| item | status | effort |
|---|---|---|
| A2 | NOT_DONE | external-dependency |
| A4 | NOT_DONE | cheap-compute |
| B4 | NOT_DONE | text-only |
| C1 | NOT_DONE | heavy-compute |
| C6 | NOT_DONE | heavy-compute |
| C9 | NOT_DONE | external-dependency |
| E2 | NOT_DONE | cheap-compute |
| A1 | PARTIAL | cheap-compute |
| A3 | PARTIAL | heavy-compute |
| A5 | PARTIAL | text-only |
| B1 | PARTIAL | cheap-compute |
| B2 | PARTIAL | cheap-compute |
| B3 | PARTIAL | heavy-compute |
| C10 | PARTIAL | cheap-compute |
| C2 | PARTIAL | text-only |
| C3 | PARTIAL | text-only |
| C4 | PARTIAL | heavy-compute |
| C5 | PARTIAL | cheap-compute |
| C7 | PARTIAL | external-dependency |
| C8 | PARTIAL | cheap-compute |
| D7 | PARTIAL | text-only |
| E1 | PARTIAL | cheap-compute |
| E3 | PARTIAL | cheap-compute |
| E4 | PARTIAL | cheap-compute |
| E5 | PARTIAL | heavy-compute |
| F1 | PARTIAL | cheap-compute |
| F3 | PARTIAL | text-only |
| D1 | DONE | none |
| D2 | DONE | none |
| D3 | DONE | none |
| D4 | DONE | none |
| D5 | DONE | none |
| D6 | DONE | none |
| D8 | DONE | none |
| F2 | DONE | none |
| F4 | DONE | none |
| F5 | DONE | none |

## A2 — NOT_DONE (external-dependency)

**Remaining:**
- Install the dependency chain: NCBI BLAST+ (makeblastdb/blastn) and hashFrag from github.com/de-Boer-Lab/hashFrag. Record exact versions for the software-environment paragraph at paper/supplementary.tex:241-246, which currently lists only MMseqs2 18-8cc5c and dustmasker 1.0.0.
- Write audit/experiments/exp_hashfrag_headtohead.py. Model the subprocess/tempdir/FASTA-export pattern on audit/experiments/exp_alignment.py:34-41 (mmseqs_cluster), which already handles seq -> FASTA -> external CLI -> per-sequence cluster vector.
- CRITICAL FASTA REQUIREMENT: hashFrag expects BOTH forward and reverse-complement orientations of every sequence in the input FASTA. Emit two records per sequence (e.g. `>{id}_f` and `>{id}_r`) or hashFrag will silently under-detect. Verify against hashFrag docs before the production run.
- Run hashFrag `filter_existing_splits` on the as-shipped train/test split of all EIGHT Genomic Benchmarks datasets, not just the two in expkit.LEAKY: human_nontata_promoters, human_enhancers_ensembl, demo_coding_vs_intergenomic_seqs, human_ocr_ensembl, demo_human_or_worm, drosophila_enhancers_stark, human_enhancers_cohn, human_ensembl_regulatory (3-class). Use the dataset list in results/leakage_report_card.csv as the canonical eight.
- Emit results/hashfrag_headtohead.csv with one row per dataset: dataset, n_train, n_test, hashfrag_leak_fraction, hashfrag_verdict, our_leak_at_0p7, our_verdict, verdict_agree (bool), wall_clock_sec, blast_wall_clock_sec, n_threads. Join our_leak_at_0p7 and our_verdict directly from results/leakage_report_card.csv (e.g. nontata 0.406 LEAKY, ensembl 0.384 LEAKY, demo_coding 0.078 borderline, human_ocr_ensembl 0.010 clean, demo_human_or_worm 0.012 clean, stark 0.016 clean, cohn 0.001 clean, ensembl_regulatory 0.005 clean).
- Emit results/hashfrag_confusion.csv: the 2x2 confusion between our 8-mer Jaccard flag (Jaccard >= 0.7) and hashFrag's Smith-Waterman-scored homolog set, per dataset, with TP/FP/FN/TN plus precision, recall, and Jaccard-index-of-agreement between the two flag sets. Reuse the band-stratified Horvitz-Thompson reweighting already implemented in audit/experiments/exp_detector_specificity.py (BANDS/TARGET/Reservoir, lines ~48-80) so the recall denominator stays estimable rather than enumerating all ~2.4e8 pairs.
- Record wall-clock explicitly: wrap each hashFrag stage in time.perf_counter() and log thread count. Report total CPU-hours per dataset. Nothing in the current bundle reports any homology-tool runtime.
- FIRST fix the incomplete prior run: audit/experiments/exp_detector_specificity.py never produced results/detector_specificity_measured.csv and covers only human_nontata_promoters. Re-run it for human_enhancers_ensembl and let summarise() finish, so the Biopython-SW arm becomes a usable internal cross-check against hashFrag rather than a dangling partial artifact.
- Rewrite paper/supplementary.tex:373-375 and paper/main_resubmission.tex:249. Both currently argue 'Specificity cannot be settled by pairwise sampling... on the order of $10^{7}$ pairs. We therefore argue the estimator floor from construction and provenance.' Once the hashFrag confusion matrix exists this argument is superseded and must be replaced with the measured precision/recall/FPR, not left standing alongside it.
- Add a new supplementary subsection (adjacent to the MMseqs2 arm at supplementary.tex:151-161) presenting the hashFrag head-to-head: the eight-row verdict-agreement table, wall-clock column, and the confusion matrix. Add a one-to-two-sentence summary in main_resubmission.tex near :222 or :249 where the MMseqs2 arm is already summarized.
- Add a `**hashFrag head-to-head**` paragraph under R1.3 in paper/response_to_reviewers.md (currently starts at :256 and covers only MMseqs2 + thresholds). State the agreement rate on all eight datasets and cite the new supplementary subsection.
- Cite hashFrag as a TOOL WE RAN, with version/commit, in the software-environment paragraph at paper/supplementary.tex:241-246, separate from its existing related-work citation at main_resubmission.tex:51.
- INDEPENDENT ADJACENT FIX: paper/references.bib:12-15 asserts main_resubmission.tex cites rafi2026, but main_resubmission.tex:51 and :172 both cite rafi2025 and paper/main_resubmission.bbl:140 shipped the v1 title. Switch both citation keys to rafi2026 and rebuild, or correct the misleading bib comment. Ledger D061 flagged this; the fix never landed.

**Notes:** Clear NOT_DONE on the item as written: hashFrag - the entire subject of the item - was never obtained, installed, or run, and none of the three requested deliverables (per-dataset verdict agreement, wall-clock, Jaccard-vs-SW confusion) exists anywhere in the bundle. The ~258 hashFrag mentions in EXECUTION_PLAN.md are a plan, and the repo's own ledger (D062) diagnoses exactly this failure mode: 'Every agent read the manuscript, the code and the CSVs; none read the field.'

Do not be misled into grading this PARTIAL on the strength of the MMseqs2 arm. That arm (results/exp_alignment.csv, supplementary.tex:151-161, response_to_reviewers.md R1.3) is a DIFFERENT tool measuring a DIFFERENT quantity (accuracy-drop reproduction) on 2 of 8 datasets. It answers reviewer R1.3's 'validate with an alignment-based measure'; it does not answer 'run hashFrag head-to-head'.

Genuinely reusable substrate worth ~30-40% of the confusion-matrix work: results/detector_specificity_pairs.csv already holds 4,300 pairs with jaccard8/flagged alongside sw_score/sw_identity/sw_coverage/homolog, and exp_detector_specificity.py already implements the band-stratified Horvitz-Thompson reweighting that makes recall estimable without enumerating ~2.4e8 pairs. Three caveats the main thread must not gloss: it uses Biopython PairwiseAligner not hashFrag; it covers 1 of 8 datasets (human_nontata_promoters only, though its docstring intended two); and it never finished - results/detector_specificity_measured.csv does not exist, so no precision/recall/FPR was ever computed. Reuse the machinery, not the conclusions.

Two contradictions the main thread should resolve while here. (1) The manuscript currently argues at supplementary.tex:373-375 and main_resubmission.tex:249 that per-pair specificity is unmeasurable; exp_detector_specificity.py was written expressly to refute that and half-ran. Whichever way this item is resolved, the manuscript and the code must stop disagreeing. (2) supplementary.tex:383-384's 'measured per-pair false-positive rate 0.000 on all three clean real-DNA panels' comes from results/estimator_specificity_real.csv, which has no alignment column at all - it is random-pair Jaccard sampling. It reads like an alignment-adjudicated result but is not one, and must not be offered to a reviewer as the requested confusion analysis.

Effort is external-dependency because hashFrag + BLAST+ must be fetched and installed before any compute starts; once installed the run itself is heavy-compute (hashFrag BLAST is CPU-bound; EXECUTION_PLAN.md:129 estimates a few CPU-hours per dataset, and human_ocr_ensembl at 174,756 rows and human_ensembl_regulatory at 289,061 rows will dominate). Budget roughly a CPU-day for all eight plus the doubled forward+reverse FASTA. Unrelated minutes-level fix bundled in remaining: the rafi2025/rafi2026 citation-key contradiction at references.bib:12-15.

## A4 — NOT_DONE (cheap-compute)

**Remaining:**
- Add bib entries to paper/references.bib for the four upstream primary sources currently named only in code strings: Umarov & Solovyev 2017 (non-promoter negatives for human_nontata_promoters), Dreos et al. EPDnew (2013/2015, promoter positives), Cohn et al. 2018 (human_enhancers_cohn), and the Stark lab Drosophila enhancer source. Suggested keys: umarov2017promoter, dreos2015epdnew, cohn2018enhancers, stark2014enhancers.
- Add a bib entry for the Genomic Benchmarks construction notebooks as a citable software/dataset artifact, e.g. @misc{gbnotebooks, note={ML-Bioinfo-CEITEC/genomic_benchmarks, docs/<dataset>/create_datasets.ipynb}, url={https://github.com/ML-Bioinfo-CEITEC/genomic_benchmarks/tree/main/docs}, urldate={2026-07-20}} so per-dataset notebook citations can be made in Table 2 and in the manipulation paragraph.
- Extend audit/experiments/exp_reference_provenance.py (or write a new audit/experiments/exp_gb_notebook_audit.py) to actually FETCH each of the four create_datasets.ipynb files from raw.githubusercontent.com and parse their cells, emitting a new results/gb_notebook_construction.csv with columns: dataset, notebook_url, http_status, access_date, negative_construction_verbatim (the random-interval sampling code/text), seq2loc_used (bool), dedup_call_found (bool), and dedup_search_tokens (the exact identifiers searched for, e.g. drop_duplicates, merge, bedtools merge, cd-hit, CD-HIT-EST, unique, set(), groupby). The absent-dedup claim must be evidenced by a recorded NEGATIVE search over an enumerated token list, not by assertion.
- Using that CSV, rewrite paper/main_resubmission.tex:68 so 'the merge step its curators omitted' carries a citation to the specific notebook plus the recorded negative dedup search — converting it from inference to citation, which is the explicit ask of A4.
- Rewrite the Table 2 caption at paper/main_resubmission.tex:122 to partly lift 'Retrospective': state that the construction categories for the datasets whose notebooks document them are taken from Genomic Benchmarks' own published methods and construction notebooks (not assigned post hoc), and confine the 'retrospective' caveat to the rows that remain inferred. Do NOT lift it globally.
- Correspondingly soften paper/main_resubmission.tex:112, which currently reads 'reproduces every verdict (Table~\ref{tab:provenance}), but retrospectively.' — distinguish notebook-documented rows from inferred rows rather than labelling all eight retrospective.
- Add a negatives column (or a caption sentence) to Table 2 at paper/main_resubmission.tex:120-136 recording the GB paper's own stated negative construction for the positive-only datasets human_enhancers_ensembl and human_ocr_ensembl: random genome intervals of matched length not overlapping any positive. Table 2's header at line 125 is currently 'Construction of positives' only, so the negative-side construction has no home in the table.
- Fix the Table 2 nontata row at paper/main_resubmission.tex:128 ('h. nontata_promoters & fixed window, recurring loci & none & LEAKY'). Under a positives-only header this attributes the defect to the wrong class: main_resubmission.tex:110 reports R_self=0.961 in the NEGATIVE class against 0.047 positive, and audit_recovered/all_findings.md:180 identifies the redundant negatives as Umarov & Solovyev's non-promoter set, not EPDnew. Either mark the row as negative-class or add an explicit class annotation.
- Name seq2loc as the candidate mechanism at paper/supplementary.tex:189-191, where the text currently stops at 'That is window geometry.' State that human_nontata_promoters is a sequence-distributed dataset remapped to reference by the seq2loc tool in the genomic-benchmarks package, and that this remapping is a plausible mechanism generating 251bp windows tiling one 1,421bp locus at 2bp median step. Mirror the addition at paper/main_resubmission.tex:222 and paper/supplementary.tex:339, which carry the same geometric-only phrasing.
- Also apply the seq2loc grounding to human_enhancers_cohn, the other sequence-distributed dataset A4 names — its Table 2 row at paper/main_resubmission.tex:131 currently claims 'curated, deduplicated / curation', which audit_recovered/all_findings.md:208 shows is unsupported upstream (cohn's README documents only a seq2loc import). Same problem at line 132 for drosophila_enhancers_stark (README documents only weak-enhancer exclusion plus a dm3->dm6 pyliftover). Either re-derive both labels from the notebooks or mark them explicitly as inferred-from-absence-of-observed-duplicates, since as written they are circular with the statistic they are meant to corroborate.
- Verify before publishing the seq2loc drop figure recorded in audit_recovered/all_findings.md:180 ('11,257 of 27,731 upstream negatives (41%) were silently dropped because seq2loc could not locate them in GRCh38') — this is an undocumented selection filter on the exact class carrying the leakage and is a strong addition to the mechanism argument, but it currently exists only in the findings file with no CSV or script backing it. Do not put it in the manuscript until a script reproduces it.
- Update audit/experiments/exp_construction.py:63-66, whose comment claims the taxonomy was 'read from the GB construction notebooks' while the PROVENANCE dict at lines 67-112 is hardcoded with no URL or fetch. Add a notebook_url and evidence field per dataset and emit them into results/construction_provenance.csv so the table's provenance claims are traceable.
- Append a decision record to audit_recovered/decisions.jsonl for A4 — there is currently none, so no prior reasoning about this item is recoverable.
- Either cite andersson2014fantom5 (paper/references.bib:137) where human_enhancers_ensembl's FANTOM5 provenance is discussed (main_resubmission.tex:108-112 is the natural site), or drop the entry — it is currently uncited in both main_resubmission.tex and supplementary.tex.

**Notes:** VERDICT RATIONALE: PARTIAL, but only barely — and the partial credit is somewhat accidental. Exactly one artifact touches A4 (results/reference_provenance.csv, which does carry the four create_datasets.ipynb URLs, network-verified at HTTP 200 on 2026-07-20). But the script that produced it self-identifies as A5 in its own docstring (exp_reference_provenance.py:3), uses the notebooks solely to pin the Ensembl release (97/100), and never fetches or parses notebook contents. None of A4's three consequences reach the manuscript: the notebooks are cited nowhere in the bundle, seq2loc appears in zero .tex/.bib/.py/.csv files repo-wide, and the 'Retrospective' label at main_resubmission.tex:112 and :122 is completely untouched. If the orchestrator wants a stricter reading, NOT_DONE is defensible — no A4-motivated work exists, only an A5 by-product that happens to contain a URL A4 needs.

NOT SUPERSEDED: the bundle has not changed in any way that dissolves this. Table 2 is present and prominent at main_resubmission.tex:120-136, the 'retrospective' caveat is load-bearing (it is the stated reason the table is 'not a prospective test'), and main_resubmission.tex:112 explicitly leans on it to defer the causal claim to §3.3. Grounding the labels in GB's published methods is exactly what would let that caveat be narrowed.

TWO CONTRADICTIONS THE MAIN THREAD MUST HANDLE, both already documented in audit_recovered/all_findings.md but never propagated into the bundle:

(1) Table 2 line 128 attributes human_nontata_promoters' defect to the POSITIVE class under a 'Construction of positives' header, while main_resubmission.tex:110 reports the redundancy is R_self=0.961 in the NEGATIVE class vs 0.047 positive. all_findings.md:180 names the correct source (Umarov & Solovyev non-promoters, not EPDnew). The manuscript currently contradicts itself across lines 110 and 128.

(2) Table 2 lines 131-132 label human_enhancers_cohn and drosophila_enhancers_stark 'curated, deduplicated / curation'. all_findings.md:208 shows upstream GB docs document no deduplication for either — cohn's README documents only a seq2loc import, stark's only weak-enhancer exclusion plus a pyliftover. Those two labels appear to be inferred from the absence of observed duplicates, i.e. from the very statistic Table 2 is meant to independently corroborate. This is a circularity in 2 of 8 rows, and it sits directly in A4's path: grounding the table in published methods will EXPOSE it rather than fix it. Expect Table 2 to get weaker, not stronger, in those two rows once the notebooks are actually read. Budget for that outcome — the honest result of A4 may be lifting 'retrospective' for the 4 notebook-documented rows while newly flagging cohn and stark as inferred.

ALSO: references.bib is missing entries for all four primary sources (Umarov, Dreos/EPDnew, Cohn, Stark) that exp_construction.py and construction_provenance.csv name as authoritative — per-dataset citation is impossible until those are added. andersson2014fantom5 (references.bib:137) is an orphan entry, cited in neither main_resubmission.tex nor supplementary.tex.

The 41% seq2loc drop rate (11,257 of 27,731 negatives unlocatable in GRCh38, all_findings.md:180) is the single strongest piece of mechanism evidence available for A4(ii), but it has NO script and NO CSV behind it — it exists only as prose in a findings file. It must be reproduced before it goes in the manuscript.

Effort is cheap-compute rather than text-only because the 'show the absent dedup step' requirement cannot honestly be met by editing prose: it needs a script that fetches the four notebooks and records a negative search over an enumerated token list. That is a small network job (4 files) plus manuscript edits.

## B4 — NOT_DONE (text-only)

**Remaining:**
- paper/main_resubmission.tex:27 — replace the title line with the conjunctive form: \title[Overlapping construction and a position-blind split]{Overlapping construction plus a position-blind split: a curation defect in genomic sequence benchmarks and the model rankings it inverts}
- paper/main_resubmission.tex:1 — update the header comment to match the new title, e.g. '%% Overlapping construction plus a position-blind split.'
- paper/main_resubmission.tex:36 — rewrite the abstract Results lead sentence to the conjunctive condition and scope the exclusive claim. Suggested: 'Near-duplicate leakage here requires two conditions together---overlapping construction and a split that ignores genomic position---rather than any property of the sequence type: across the human benchmarks audited here it tracks assembly, not content.' Keep the rest of the Results block byte-identical so no number moves.
- paper/main_resubmission.tex:55 — rewrite the Introduction thesis sentence from the unqualified 'Leakage tracks how a dataset was assembled, not what kind of sequence it contains.' to the scoped conjunctive form, and add the one-clause exception pointer. Suggested: 'Across the human datasets audited here, leakage tracks how a dataset was assembled rather than what kind of sequence it contains; the condition is conjunctive---overlapping construction and a position-blind split---and the one case where similarity is instead a property of the genome, GUE's virus_covid, is not a curation defect (\S\ref{sec:d-detector}).' This is the 'scoped sentence' the item asks for.
- paper/main_resubmission.tex:266 — change the Conclusion tagline 'Audit construction, not sequence type.' to name both conditions, e.g. 'Audit construction and the split rule together, not sequence type.'
- paper/main_resubmission.tex:258 — after 'The condition our evidence supports is thus conjunctive: overlapping construction \emph{and} a split that ignores genomic position.', add a forward reference tying it to the new title so the Discussion and title agree explicitly.
- paper/supplementary.tex:1 — update the header comment to the new title.
- paper/supplementary.tex:29 — update the \large title line (spans lines 29-30) to the new title; keep the '\\' line break and the second line 'a curation defect in genomic sequence benchmarks and the model rankings it inverts' unchanged.
- paper/cover_letter.md:8 — update the quoted enclosed-article title to the new one. Leave the BIOADV-2026-296 prior-title reference untouched.
- paper/response_to_reviewers.md:47-48 — rewrite 'replaces it with the sharper claim the title now carries: **leakage tracks how a dataset was assembled, not what it contains.**' to state the conjunctive claim the new title carries, and add one sentence acknowledging virus_covid at 1.000 as the scoped exception, so the letter does not contradict Section 4.
- Rebuild the manuscript. NOTE: paper/build.sh compiles main.tex (the STALE 20-page file), NOT main_resubmission.tex — there is no build path for the canonical bundle. Compile main_resubmission.tex and supplementary.tex manually: export PATH="$HOME/Library/TinyTeX/bin/universal-darwin:$PATH"; cd paper; pdflatex -interaction=nonstopmode main_resubmission.tex; bibtex main_resubmission; pdflatex main_resubmission.tex twice; same for supplementary.tex.
- Re-check the 8-page budget after the edits (pdfinfo paper/main_resubmission.pdf | awk '/^Pages/{print $2}'). The new title is 3 characters shorter than the current one and the abstract/intro rewrites are length-neutral as drafted, so this should be a no-op, but confirm.
- Run paper/build_letters.sh to regenerate cover_letter.pdf/.docx and response_to_reviewers.pdf/.docx after the markdown edits; confirm the script prints missing-glyphs=0 for both.
- Append a decision entry to audit_recovered/decisions.jsonl recording the B4 retitle: node 'B4', action 'retitle-to-conjunctive', citing main_resubmission.tex:251 and results/gue_census.csv virus_covid leak_jaccard_0p7=1.0 as the reason the exclusive title could not stand, and noting it completes what D065 started in the Conclusion.

**Notes:** No experiment is required for this item — it is purely a framing/text fix, and the underlying evidence is already run and real. I verified the reviewer's premise against the source data rather than taking it on faith: results/gue_census.csv carries virus_covid at leak_jaccard_0p7=1.0 on the full partition (n_train=73335, n_test=9168, subsampled=False), and every number the manuscript quotes at main_resubmission.tex:251 (1.000, 0.122, min 0.824, median 0.987) matches the CSV exactly. The reviewer is correct on the merits: the paper's own Section 4 documents a case where leakage tracks content, which the title's 'not what it contains' denies.

Two things make this cheaper than it looks. First, the conjunctive condition is ALREADY written, verbatim, at main_resubmission.tex:258 — 'The condition our evidence supports is thus conjunctive: overlapping construction and a split that ignores genomic position.' The fix is promotion, not invention, and the reviewer's suggested title is a near-paraphrase of the paper's own sentence. Second, D065 in the ledger shows the author already accepted this exact virus_covid exception argument and acted on it in the Conclusion (rescoping 'absent from fifteen tasks' to the eleven predicted-leaky), so there is no principled resistance to overcome — the title was simply never revisited when the body was.

CONTRADICTION FOUND, worth flagging to the main thread: the title/abstract/intro/conclusion assert the exclusive dichotomy in four places with no qualifier anywhere near the assertion, while the refutation sits at line 251, deep in a Discussion paragraph, 200+ lines away. A reviewer reading front-to-back meets the claim four times before meeting its counterexample. That is the specific structural defect B4 names, and fixing only the title without the scoped sentence at :55 would leave it half-addressed.

BUILD INFRASTRUCTURE GAP, discovered while checking how to rebuild: paper/build.sh hardcodes main.tex throughout (pdflatex main.tex, bibtex main, pdfinfo main.pdf) and has no main_resubmission target. The canonical 8-page bundle has no build script. Whoever executes the remaining list must compile main_resubmission.tex and supplementary.tex by hand, and it is worth considering a separate fix that points build.sh at the canonical file — otherwise the stale main.pdf keeps getting regenerated while the real deliverable is built ad hoc. This is out of scope for B4 but is a live footgun for every remaining text item.

One judgment call I made explicit: I graded this NOT_DONE rather than PARTIAL. The pre-existing conjunctive sentence at :258 is not a response to B4 — it predates the item and coexists with the contradicting title, which is precisely the inconsistency the reviewer is objecting to. Neither half of the requested fix (retitle; scope the exclusive claim) has been performed anywhere in the bundle.

## C1 — NOT_DONE (heavy-compute)

**Remaining:**
- Run C1a: `cd /Users/rikhinkavuru/homology_audit && python -m audit.experiments.exp_manip_replication --only A 2>&1 | tee exp_manip_replication_A.log`. Produces 6 rows (control / manipulated_unmatched / manipulated_matched for each of human_enhancers_cohn at n=20,000 and drosophila_enhancers_stark at full n=6,914) into results/construction_manipulation_replication.csv. Budget ~25 min for cohn and ~20-40 min for stark, extrapolating from the `seconds` column of results/construction_manipulation.csv (ocr's three arms took 733.4 + 432.9 + 275.5 s); stark has fewer rows but 2,142 bp median sequences vs cohn's 500 bp.
- Run C1b: `python -m audit.experiments.exp_manip_replication --only B 2>&1 | tee exp_manip_replication_B.log`. Produces the single B_control_unmerged_union_matched row (human_enhancers_ensembl subsampled to n=81,868 at 50/50). Budget ~18-30 min (B_control_unmerged_union at n=154,842 took 1778.1 s; B_manipulated_merged_balanced at n=81,868 took 1077.2 s).
- Confirm the run completed rather than partially wrote: check that the log ends with the `EXP_MANIP_REPLICATION_DONE <n>s` marker (printed at exp_manip_replication.py:178) and that results/construction_manipulation_replication.csv has 7 data rows with non-null rf_drop_ci on every row.
- RETRACT the wrong claim at paper/main_resubmission.tex:68. Delete or rewrite the clause 'while the fix-a-leaky pair cannot be, since size there cannot be equalised without discarding the rows the intervention is defined to remove.' C1b disproves it: the manipulated arm cannot be grown, but the CONTROL can be subsampled to 81,868. Replace with a sentence stating both pairs are now size-matched, the fix-a-leaky pair by subsampling the control to the manipulated arm's n. Do not merely add the new arm while leaving this sentence standing -- it would contradict the new row.
- Extend the break-a-clean methods sentence at paper/main_resubmission.tex:68 from one donor to three: state that the identical construction (same TARGET=0.9426 share on a multiplicity-2 coordinate, same rate algebra r = T/(2-T) = 0.8914, same three arms, same split seed, nothing retuned per donor) was applied to human_ocr_ensembl, human_enhancers_cohn and drosophila_enhancers_stark.
- Add rows to Table 2 at paper/main_resubmission.tex:149-154 using the real numbers from results/construction_manipulation_replication.csv: one new row in the 'Fix' block for the size-matched control (n=81,868), and a 'Break' block extended to three donors (cohn control + pos. duplicated; stark control + pos. duplicated). If the 8-page limit binds, move the two extra donors to a new supplementary table and keep a one-line summary in Table 2 -- but the size-matched fix-a-leaky control must stay in the main table, since it is the arm that removes a confound from a headline number.
- Update the Table 2 caption at paper/main_resubmission.tex:145: 'the break-a-clean pair is matched on size as well' must become plural (three donors) and must state that the fix-a-leaky pair now carries a size-matched control arm too.
- Rewrite the results prose at paper/main_resubmission.tex:141 to report the replication as a design rather than an anecdote: give the leak fraction and RF drop with CI for cohn and stark alongside ocr, and state explicitly whether the ranking inversion (rf_rank_orig -> rf_rank_corr, column `ranking_inverts`) reproduced on all three donors. If a donor FAILS to invert, report that as a failure of the registered pattern -- do not bury it.
- Add one sentence at paper/main_resubmission.tex:141 (or in the Table 2 discussion at line 159) reporting the size-matched fix-a-leaky contrast: B_control_unmerged_union_matched (n=81,868) vs B_manipulated_merged_balanced (n=81,868), so the reader can see the RF drop is not an artifact of the manipulated arm training on 47% fewer rows than its control.
- Narrow the 'priced but not performed' sentence at paper/main_resubmission.tex:256 and its mirror at paper/response_to_reviewers.md:479. After C1a runs, break-a-clean on drosophila_enhancers_stark HAS been performed; only the HyenaDNA fine-tuning half remains unperformed. Reword to something like 'fine-tunes HyenaDNA on both arms of the drosophila_enhancers_stark break-a-clean of Table 2' so the paper does not simultaneously report and disclaim the same experiment.
- Register the new numbers in audit/tools/check_numbers.py so the consistency gate covers them (the tool currently has no cohn/stark manipulation entries), then re-run it and confirm it stays green.
- Append a decision entry to audit_recovered/decisions.jsonl recording that C1a and C1b ran, with evidence pointing at results/construction_manipulation_replication.csv and the two run logs.
- Rebuild the PDF and confirm Table 2 still fits the OUP 8-page Original Article limit after the added rows.

**Notes:** SCAFFOLDING IS DONE, SCIENCE IS NOT. audit/experiments/exp_manip_replication.py is a complete, careful, correct implementation of BOTH halves of C1 (written Jul 20 00:41, untracked, never committed) and every dataset it needs is already cached. But it has produced zero output: no results/construction_manipulation_replication.csv, no log, no ledger entry, no manuscript text. An experiment script existing is not evidence it ran, and here it demonstrably did not. Effort is therefore compute + write-up, not design.

CONTRADICTION THE MAIN THREAD MUST HANDLE, NOT JUST PATCH AROUND: main_resubmission.tex:68 does not merely omit the size-matched comparator, it argues the comparator is impossible -- 'the fix-a-leaky pair cannot be [size-matched], since size there cannot be equalised without discarding the rows the intervention is defined to remove.' That reasoning is wrong in exactly the way the reviewer implies, and the script's own docstring (lines 22-28) diagnoses it: the manipulated arm cannot be grown, but the control can be subsampled to 81,868. If C1b's row is added without retracting that sentence, the manuscript will assert an experiment is impossible on one page and report it on the next. Retraction is mandatory, not optional.

SECOND CONTRADICTION: main_resubmission.tex:256 (mirrored at response_to_reviewers.md:479) says break-a-clean on drosophila_enhancers_stark is 'priced but not performed.' That sentence is scoped to the HyenaDNA fine-tuning route, but once C1a runs stark it will read as a flat contradiction of Table 2. Must be narrowed to the fine-tuning half only.

THE OBJECTION IS BROADER THAN THE ITEM STATES: the ten-dose sweep (supplementary.tex:274, Fig/fig_manipulation_data.csv) also runs on human_ocr_ensembl. So the single clean donor carries the manipulation result AND the dose-response AND the phi* threshold calibration. Every causal claim in the paper traces to one donor, which strengthens the reviewer's point rather than mitigating it. Worth saying so in the response letter -- the three-donor replication buys more than Table 2.

SIBLING ITEM, SAME FAILURE MODE (out of my scope but the main thread should know): audit/experiments/exp_dose_replication.py is a 359-line script for item C6 (interval on the inversion threshold, 45 trials), written Jul 20 01:01, and its outputs results/dose_replication_trials.csv and results/dose_replication_summary.csv are also ABSENT. Same pattern: harness written, never executed. Whoever runs C1 should check whether a batch of C-item scripts was authored in one sitting and none were launched.

HONEST-REPORTING RISK: exp_manip_replication.py deliberately freezes TARGET=0.9426 and retunes nothing per donor (lines 12-17, 58-59), which is the right design -- but it means a donor is genuinely free to NOT reproduce the inversion. If cohn or stark comes back with `ranking_inverts=False` or a rf_drop_ci spanning zero, that is a real result about the generality of the mechanism and must be reported as such. Do not quietly drop a non-replicating donor or retune the dose to rescue it; a three-donor design that reports 2/3 is far stronger than a curated 3/3.

## C6 — NOT_DONE (heavy-compute)

**Remaining:**
- Run the existing module — no code needs writing: `cd /Users/rikhinkavuru/homology_audit && python -m audit.experiments.exp_dose_replication`. It writes results/dose_replication_trials.csv (45 rows: dose x split_seed x model_seed) and results/dose_replication_summary.csv (per-dose inversion probability + Clopper-Pearson CI + split-clustered CI, plus a THRESHOLD_SUMMARY row).
- Estimated ~50-70 min serial. The module already caches the expensive data-side work per (dose, split seed) — featurize, max_sim_to_train, clustering are computed once per realisation and only RF/HGB refit per model seed (LR/LinearSVC are verified deterministic at runtime via DET_CHECK). To parallelise, run split seeds concurrently with `--split-seeds 0 1 --out-suffix _a --no-summary` etc., then reconcile with `--merge`. Prefer `--rf-jobs 4` (not -1) when running concurrently.
- Read the THRESHOLD_SUMMARY row of results/dose_replication_summary.csv for phi_min / phi_max / phi_mean plus censored_below / censored_above counts. That min-max span is the interval that must replace the bare 0.106.
- Rewrite paper/main_resubmission.tex:160: replace "places the first ranking inversion at an imposed leak fraction of $0.106$ and finds none below it" with the interval form, e.g. "places the first ranking inversion in the range $\varphi\in[\mathrm{phi\_min},\mathrm{phi\_max}]$ across 15 split-seed x model-seed replicates". Also delete or amend the trailing clause "at a single fit per condition read only between arms" — it is exactly the concession C6 asks to be retired.
- Rewrite the Fig. 2 caption at paper/main_resubmission.tex:165: replace "each dose is a single fit at one split seed" with the replication design (5 split seeds x 3 model seeds at the three bracketing doses 0.25/0.30/0.35), and restate "The dashed rule marks the first inversion" as a shaded threshold band rather than a single rule.
- Rewrite paper/supplementary.tex:283-288 (\S S2, sec:s-dose): keep the ten-dose sweep as the coarse scan, then add a paragraph reporting per-dose inversion probability with binomial CIs at 0.25 / 0.30 / 0.35 and the threshold as an interval. Cite the new file by name, as the supplement already does for other CSVs (cf. supplementary.tex:463 citing results/exp_inject3class_deep.csv).
- Check whether P6 needs rescoring. paper/main_resubmission.tex:80 registers P6 as "a ranking inversion occurs at every imposed leak fraction above the diagnostic threshold ... and at none below", and main_resubmission.tex:189 defers its scoring to Supplementary S2. If replication shows inversion probability strictly between 0 and 1 at any of the three doses, P6 is no longer cleanly 'right on all ten doses' (supplementary.tex:282) and that sentence must be softened to a probabilistic statement.
- Regenerate the figure after the text edits: paper/Fig/make_fig_manip.py drives paper/Fig/fig_manipulation.{pdf,png} from paper/Fig/fig_manipulation_data.csv (443 bytes, one row per dose, columns phi,rf_drop,ci_lo,ci_hi,ranking_inverts,is_table2_arm). It has no column for inversion probability, so it needs a small extension to draw the three replicated doses as probabilities with error bars and the threshold as a band.
- Append a C6 decision entry to audit_recovered/decisions.jsonl recording the run, the observed inversion probabilities, and the interval adopted — there is currently no ledger trace for this item at all.

**Notes:** A complete, spec-matching script existing is NOT evidence of a run, and this is the textbook case: exp_dose_replication.py implements C6 point-for-point (3 bracketing doses, 5 split seeds, 3 model seeds, Clopper-Pearson, threshold-as-interval with censoring reported rather than dropped) but produced zero output. The remaining work is execution plus prose, not design — which is why I rated it heavy-compute rather than external-dependency.

MATERIAL RISK the main thread should price in before running: the replication may not vindicate 0.106. The discrete outcome turns on top-two as-shipped margins of 0.0010 at dose 0.25 and 0.0083 at dose 0.30 — both plausibly inside single-fit noise. If inversion probability at dose 0.30 comes back at, say, 0.6 rather than 1.0, then the sharp claim "inverts at all six higher doses; it does not invert at the three lower ones" (supplementary.tex:285-286) and registered prediction P6 (main_resubmission.tex:80) both weaken, and the edits in 'remaining' grow beyond swapping a number. The script's own docstring anticipates this: "If the interval is wide, that IS the finding and it is reported as such." Budget for the possibility that P6's scoring at supplementary.tex:282 changes from a clean pass to a partial.

Mitigating fact worth noting to the reviewer: the manuscript is already honest about the limitation rather than hiding it — main_resubmission.tex:160 says "at a single fit per condition" and the Fig. 2 caption says "each dose is a single fit at one split seed". So this is an undisclosed-uncertainty fix, not a correction of a misrepresentation. That framing may be useful in the response letter.

One cross-item interaction: results/dose_response.csv is also the source for the phi* diagnostic threshold (0.0602 at dose 0.30) and for Fig. 2's shaded cluster-bootstrap band. Re-running replication does not regenerate dose_response.csv, so the ten-dose sweep and the three-dose replication will coexist as two separate files; the supplement must be explicit about which figure each number comes from to avoid the kind of provenance confusion ledger entry D064 records.

## C9 — NOT_DONE (external-dependency)

**Remaining:**
- File the issue at https://github.com/ML-Bioinfo-CEITEC/genomic_benchmarks/issues under the user's own GitHub account. REQUIRES THE USER — do not file on their behalf without explicit go-ahead, and do not fabricate an issue number. Suggested content, all numbers already verified in the manuscript: title 'human_enhancers_ensembl positives contain duplicated genomic coordinates (train/test overlap)'; body reporting that 77,421 positive intervals sit on 40,934 distinct coordinates (main_resubmission.tex:274 conclusion, cover_letter.md:12), self-overlap redundancy R_self = 0.471 for human_enhancers_ensembl against 0.000-0.010 for human_ocr_ensembl and human_ensembl_regulatory (main_resubmission.tex:112), near-duplicate test leak fraction 0.390 at Jaccard 0.7, and that applying a coordinate merge step collapses the leak fraction to 0.012 and random-forest inflation from +0.165 to -0.008 (cover_letter.md:12). Link the preprint/repo and offer the splitter.
- Record the exact filing DATE and the returned ISSUE NUMBER. Note that 'any response' from maintainers may not arrive before resubmission — if there is no response yet, the manuscript sentence must say so explicitly ('no response at time of submission') rather than omit it.
- Add a new back-matter section to /Users/rikhinkavuru/homology_audit/paper/main_resubmission.tex, inserted between \section*{Conflict of Interest} (line 271) and \section*{Data Availability} (line 274). Suggested single sentence: '\\section*{Responsible Disclosure}\\nThe coordinate-level findings of \\S\\ref{sec:r-coord} were reported to the Genomic Benchmarks maintainers as issue \\#NNN on DD Month 2026 (\\url{https://github.com/ML-Bioinfo-CEITEC/genomic_benchmarks/issues/NNN}); [their response is summarised here / no response had been received at the time of submission].' Fill NNN and the date from the real issue — leave the placeholder in and flag it if the issue has not actually been filed.
- Mirror the same statement in /Users/rikhinkavuru/homology_audit/paper/response_to_reviewers.md as an explicit reply to the ethics point, since that is where the reviewer will look for it. The letter currently has no ethics response at all.
- Address the second, unexecuted half of REVIEWER_RESPONSE_PLAN.md:151: add one clause stating whether homology/near-duplicate control was ever a stated design goal of Genomic Benchmarks. The natural home is the Discussion, near main_resubmission.tex:232 which already discusses the suite's own published baseline and cites gresova2023. If Gresova 2023 never claimed homology control, saying so plainly is a fairness concession that costs nothing and blunts the 'you are calling their work defective' objection.
- Optional but cheap given the title now says 'defect': consider whether the Conflict of Interest section (main_resubmission.tex:271-272, currently 'None declared.') should remain untouched — it should, this belongs in a separate Responsible Disclosure section, not under COI.

**Notes:** HARD BLOCKER: this is the one item on the list that cannot be completed inside the repo. It requires a real action on a third party's GitHub (the user's account, a real issue number, a real date), and the 'any response' clause depends on the CEITEC maintainers replying. The main thread should NOT mark this DONE by writing a plausible-looking sentence — an invented issue number or date in a submitted manuscript is a research-integrity problem strictly worse than the omission it papers over. If the user will not file the issue before resubmission, the honest fallback is a sentence stating the finding has been reported and no response was received at the time of submission, written only AFTER the issue actually exists.

CONTRADICTION WORTH FLAGGING: the item's exposure has grown since the reviewer raised it. At review time 'defect' was body text; it is now in the title (main_resubmission.tex:27) and appears 19 times. cover_letter.md:12 shows this was an intentional sharpening. A reviewer who raised the ethics point on the prior round and sees the accusation promoted to the title with still no disclosure note is likely to escalate rather than drop it. This raises the item's priority above 'minor textual'.

UNRELATED ISSUE FOUND, OUTSIDE C9'S SCOPE, BUT INTERACTS WITH IT: paper/main_resubmission.pdf is 15 pages, not 8 — confirmed at paper/main_resubmission.log final line, 'Output written on main_resubmission.pdf (15 pages, 413322 bytes)'. audit_recovered/decisions.jsonl line 23 (D022) records the 8-page cap as independently verified against two OUP pages for Bioinformatics Advances Original Articles. Whatever the explanation (the count may include the bibliography or reflect a non-final layout), the main thread should be aware that 'add a sentence' lands in a manuscript that currently overruns its verified cap by 7 pages. Someone should own that separately.

METHOD NOTE: I checked for the trap the brief warned about — an experiment existing without running, or a CSV existing without being cited. Neither applies here, because C9 requires no experiment and no number. The check that matters for this item is purely presence-of-text in the bundle, and the text is absent from all five bundle files, from the stale main.tex, from the ledger, and from git history. I found no evidence in any direction that this was ever started.

## E2 — NOT_DONE (cheap-compute)

**Remaining:**
- Write audit/experiments/exp_bca_check.py (numpy-only; scipy is NOT installed in this environment, confirmed by `python3 -c "import scipy"` -> ModuleNotFoundError, so hand-roll the jackknife rather than calling scipy.stats.bootstrap). Re-use audit/core/expkit.py cluster_boot (line 260) to get the draws, then compute BCa: z0 = Phi^-1(mean(draws < theta_hat)); acceleration a from LEAVE-ONE-CLUSTER-OUT jackknife over the same components (a = sum(d_i^3) / (6 * (sum(d_i^2))^1.5) where d_i = mean(jack) - jack_i); adjusted percentiles alpha1/alpha2 = Phi(z0 + (z0 -/+ 1.96)/(1 - a*(z0 -/+ 1.96))). Implement Phi/Phi^-1 via math.erf and an inverse-normal rational approximation since scipy is absent.
- Run that script at BOOT=10000 with seed 20240524 (matching audit/experiments/exp_clusterboot_full.py:15) over, at minimum: (a) the two leaky-dataset RF drops (human_nontata_promoters, human_enhancers_ensembl), (b) the same two for LR, (c) drosophila_enhancers_stark RF as the smallest-G case, and (d) the borderline verdicts the interval-statistician lens named at audit_recovered/all_findings.md:371 — the CNN-vs-LinearSVC paired interval (results/exp_deep_cnn_paired.csv), the clean-dataset nulls, and the splice_sites tasks — because those sit closest to zero where first-order percentile error actually matters.
- Have the script emit results/bca_vs_percentile.csv with columns dataset, model, estimand, theta_hat, n_clusters, skewness, z0, accel, ci_percentile_lo, ci_percentile_hi, ci_bca_lo, ci_bca_hi, max_abs_shift, verdict_flips. This CSV is the artifact that currently does not exist and whose absence is what blocks the item.
- Verify against the recorded claims in audit_recovered/INDEPENDENT_TRACKS.md:468-471 before quoting anything: nontata RF percentile [0.09148,0.12563] vs BCa [0.09207,0.12602] (shift 0.00059), nontata LR 0.00005, ensembl RF 0.00035, ensembl LR 0.00005, stark RF 0.00047, and skewness <= 0.04 in absolute value. Do NOT copy these five numbers into the manuscript unless the script reproduces them — decisions.jsonl D064 records the main thread doing exactly that with 'twentyfold' and it reaching main_resubmission.tex:230.
- Edit paper/main_resubmission.tex:91, appending to the existing sentence "Reported $95\%$ CIs are percentile bootstrap intervals over $10{,}000$ draws with the resampling unit stated per number": a clause stating the percentile choice was checked, not assumed - the bootstrap distributions are near-symmetric (max |skewness| = <VALUE>), and recomputing <N> intervals including the smallest-cluster-count case as bias-corrected-and-accelerated moves no bound by more than <VALUE>, so percentile and BCa agree and the simpler is reported. Use the numbers the script actually emits. Adapt the drafted sentence at audit_recovered/INDEPENDENT_TRACKS.md:466 rather than pasting it, since it was written against the stale 20-page main.tex:178 and its \S\ref targets and surrounding prose do not match the 8-page structure.
- Add the BCa method definition to paper/supplementary.tex in \subsection{Imbalanced evaluation, tuning selection, and robustness definitions} (label sec:s-defs, line 165), beside the existing \paragraph{Design effect.} block at lines 195-201: give the z0 and acceleration formulas, state the jackknife is leave-one-cluster-out (not leave-one-sample-out, to match the block-bootstrap resampling unit), and cite results/bca_vs_percentile.csv.
- Add a bullet to the change-summary list in paper/response_to_reviewers.md immediately after the "Bootstrap methodology" bullet at lines 93-97, stating that interval TYPE as well as resampling UNIT was checked, with the max BCa-vs-percentile shift and the statement that no verdict changes. The existing bullet answers R3.3 on the unit only and is silent on the type.
- Add efron1987bca (Efron, 'Better Bootstrap Confidence Intervals', JASA 82(397):171-185, 1987) to paper/references.bib and cite it at the new main_resubmission.tex:91 clause. Check the reference count stays within the 50-reference OUP limit recorded at audit_recovered/decisions.jsonl D022 (was 34/50 at D023).
- Append a decision entry to audit_recovered/decisions.jsonl recording E2: what was computed, the CSV path, the max shift, and whether any verdict flipped. No E2/BCa entry currently exists in any of the 66 lines.
- After editing, run audit/tools/check_numbers.py so the new skewness and max-shift figures are gated - note decisions.jsonl D058 records that check_numbers.py:49 was reading paper/main.tex (the stale reference copy) rather than main_resubmission.tex, so confirm it now reads the 8-page bundle before trusting a pass.

**Notes:** Headline: the manuscript bundle contains ZERO trace of BCa. Every reported CI in the paper is percentile, produced by a single 2-line helper (expkit.py:273-274) duplicated at cluster_bootstrap.py:81. So on the manuscript axis this is effectively NOT_DONE.

I graded it PARTIAL rather than NOT_DONE for one reason: audit_recovered/INDEPENDENT_TRACKS.md:462-471 shows the analysis was reportedly performed in a prior pass, and contains both concrete BCa numbers and a fully drafted replacement LaTeX sentence. That is real work-product. But it was never applied, and — this is the important caveat — it is UNREPRODUCIBLE. There is no BCa code, no jackknife code, no skewness computation, and no CSV anywhere in the repo. The five claimed shift figures (0.00059 / 0.00005 / 0.00035 / 0.00005 / 0.00047) exist solely as prose in a planning markdown file.

Treat those numbers as UNVERIFIED, not as a result to transcribe. The repo has a documented precedent for exactly this failure: decisions.jsonl D064 is the main thread's own admission that it invented "twentyfold", propagated it through prompts as established fact, and an agent wrote it into main_resubmission.tex:230. Re-running the computation is cheap (no model refits — it reuses stored per-example correctness vectors and cluster labels, so it is O(N) plus the bootstrap loop) and is strictly safer than trusting the note.

Two partial corroborations worth knowing: (1) the claimed percentile side for nontata RF, [0.09148,0.12563], does match results/cluster_bootstrap_full.csv:2's [0.0915, 0.1256] to 4 dp, so whoever wrote the note had the real percentile draws in hand; (2) the claimed effective sample size of 1,710 for the stark corrected arm is arithmetically consistent with results/cluster_stats.csv (6,826 components x test_frac 0.25036 = 1,709), though the n_clusters field for stark is empty in cluster_bootstrap_full.csv. Neither corroborates the BCa side.

Scope caution: the item as the reviewer framed it says "for the skewed delta distributions". The interval-statistician lens (all_findings.md:371) was more specific and more useful — it named the CNN paired interval, the clean-dataset nulls, and the splice_sites tasks as the verdicts sitting close enough to zero that first-order percentile error could matter. A BCa check run only on the two headline leaky-dataset drops (which have ~7.6k and ~29k clusters and huge effect sizes) would be the easy case and would not actually discharge the objection. The stated escape hatch — "or state that percentile and BCa agree" — still requires the computation to have been done and be citable.

Environment note for whoever executes this: scipy is NOT installed, so scipy.stats.bootstrap(method='BCa') is unavailable. The BCa must be hand-rolled in numpy.

No version-gap on this item: paper/main.tex:178 (the stale 20-page version) is also percentile-only, so this is not a case of something fixed in the old file and lost in the restructure.

One thing I checked and cleared, so nobody re-chases it: main_resubmission.tex:91 claims "10,000 draws", and audit/experiments/cluster_bootstrap.py:24 sets BOOT = 1000, which looked like a contradiction. The headline intervals trace to exp_clusterboot_full.py (BOOT = 10000 at line 15) and match cluster_bootstrap_full.csv — e.g. main_resubmission.tex:218's [0.155,0.173] matches that file's [0.1552,...], not the 1000-draw file's [0.1579,...]. results/cluster_bootstrap.csv appears to be a superseded 1000-draw artifact. Low priority, but if the E2 edit touches line 91 anyway, it may be worth confirming no surviving quoted interval traces to the 1000-draw file.

## A1 — PARTIAL (cheap-compute)

**Remaining:**
- Replace both v1 citations with v2 in the canonical bundle: paper/main_resubmission.tex:51 change \citep{rafi2025} to \citep{rafi2026}, and paper/main_resubmission.tex:172 change \citep{kapoor2023leakage,rafi2025,bushuiev2024} to \citep{kapoor2023leakage,rafi2026,bushuiev2024}.
- Fix the false provenance comment at paper/references.bib:12-15. It currently asserts main_resubmission.tex cites rafi2026 and that its bibliography carries v2 alone; both were untrue at audit time. Either delete the comment or rewrite it to describe the post-fix state, and only after the main_resubmission.tex edits above are actually made.
- Run the purpose-built experiment, which has never executed: `cd /Users/rikhinkavuru/homology_audit && PYTHONPATH=. ./venv/bin/python audit/experiments/exp_graded_shape.py 2>&1 | tee exp_graded_shape.log`. Confirm it produces results/graded_shape.csv and results/graded_shape_summary.csv. Note it runs the two leaky datasets at FULL scale (cap=10**12) across all nine MODEL_ORDER learners, so budget real wall time.
- Read results/graded_shape_summary.csv columns shape_balanced_accuracy and shape_auroc (NOT shape_raw_accuracy -- raw is prevalence-confounded per main_resubmission.tex:82 and graded_gap_corrected.csv:2). Determine for each of the two leaky datasets whether the balanced-accuracy curve is monotone increasing or shows an interior DIP.
- Write the Rafi-contrast sentence into paper/main_resubmission.tex sec:r-mech (at or just after line 192, where the graded gap is currently presented uncited), citing rafi2026 and reporting the shape verdict with its real numbers from graded_shape.csv. Anticipated content from the existing raw data, to be confirmed or overturned by the balanced run: human_nontata_promoters is monotone increasing for all four models across four well-populated bins (n = 4971/392/1634/2037; RF 0.8793 -> 0.9082 -> 0.9933 -> 1.0000), and on human_enhancers_ensembl the memorizing RF is likewise monotone (0.7698 -> 0.9325 -> 0.9391 -> 1.0000). If the balanced curves show no interior dip, state that our monotone gradient differs from the non-monotonic curve Rafi et al. report and say so as a result, per the item's instruction.
- If the balanced-accuracy run reproduces the interior PEAK now visible in raw accuracy for LR/LinearSVC/HGB on human_enhancers_ensembl (LR 0.7649 -> 0.9132 -> 0.8957 -> 0.8019), report it explicitly and note that it is an interior PEAK -- best at intermediate similarity -- i.e. the mirror image of Rafi's interior DIP, not agreement with it. Do not let it be described loosely as 'also non-monotonic'.
- Add one qualifying clause at paper/main_resubmission.tex:192 attributing graded memorization-by-similarity to rafi2026, so the section does not imply the graded gap is novel here. Line 172 currently concedes only Claim I (de-leaking lowers accuracy); the v2 retitle ('...and memorization...') means the memorization gradient is theirs too and needs its own concession.
- Re-read paper/main_resubmission.tex:55 after the above edits. It already claims exactly the four items the reviewer says to keep (coordinate-space diagnosis, bidirectional construct-and-break, falsified task-type registration, ranking inversion) and claims nothing about graded memorization, so it likely needs no change -- confirm rather than rewrite.
- Add a line to paper/response_to_reviewers.md answering A1: neither it nor cover_letter.md currently contains any occurrence of 'Rafi', 'v2', or 'version 2'. Disclose the re-cite, the narrowed novelty claim, and the shape contrast.
- Rebuild the bundle (paper/build.sh) and verify paper/main_resubmission.bbl no longer contains the 2025 v1 title 'Detecting and avoiding homology-based data leakage in genome-trained sequence models' and instead carries the v2 'Characterizing homology-induced data leakage and memorization...'.
- Append a decision line to audit_recovered/decisions.jsonl resolving A1, and flip D061 (line 62) status from "open" -- noting that D061 also lists four further stale entries (zhou2023dnabert2, bushuiev2024, kovtun2024, huang2025) that are OUTSIDE A1's scope and remain open.

**Notes:** Headline: A1 is PARTIAL, and the partial credit is almost entirely groundwork rather than delivered text. Three things are real and done: the rafi2026 bib entry exists and is correctly formed (references.bib:24-32); the Introduction delta paragraph at main_resubmission.tex:55 already claims exactly the four items the reviewer says to keep and makes no graded-memorization novelty claim; and a purpose-built experiment script exists. Everything else is undone.

TRAP 1 -- a false comment asserts the work is done. paper/references.bib:12-15 states 'The current submission (main_resubmission.tex) cites rafi2026, the v2 of the same preprint. BibTeX emits only cited entries, so main_resubmission's bibliography carries v2 alone.' Every clause of that is false. main_resubmission.tex cites rafi2025 at lines 51 and 172 and cites rafi2026 zero times; main_resubmission.bbl:140-144 carries the v1 entry alone. Anyone grepping references.bib and trusting the comment would mark A1 done. Do not; and the comment itself is a reviewer-visible defect that must be corrected or removed.

TRAP 2 -- the experiment script is not evidence of a run. audit/experiments/exp_graded_shape.py is written specifically for A1 (its docstring names 'A1 (Rafi v2 contrast)') but has NEVER EXECUTED. Neither results/graded_shape.csv nor results/graded_shape_summary.csv exists, no log matches *graded_shape*, and the string appears in no other file in the repo. This is the exact case the brief warned about.

TRAP 3 -- do not write the contrast sentence from results/graded_performance.csv. That CSV holds RAW per-bin accuracy, and the manuscript itself declares the raw statistic confounded (main_resubmission.tex:82; graded_gap_corrected.csv:2 gives pos_rate 0.9997 in the high bin vs 0.1968 in the novel bin on human_enhancers_ensembl, constant_classifier_raw_gap 0.8029). The non-monotonic interior peak visible in raw accuracy for LR/LinearSVC/HGB on that dataset is plausibly pure prevalence artifact. exp_graded_shape.py exists precisely to redo this with balanced accuracy; run it first.

SUBSTANTIVE FINDING the main thread should know: the existing data suggests our answer to the reviewer is 'no dip, and in one case the opposite of a dip'. human_nontata_promoters is cleanly monotone increasing for all four models with all four bins well populated (n = 4971/392/1634/2037) -- the strongest contrast case, since Rafi report non-monotonicity. On human_enhancers_ensembl the memorizing RF is monotone (0.7698 -> 0.9325 -> 0.9391 -> 1.0000) while the three non-memorizers show an interior PEAK, best at intermediate similarity and worse at both extremes. Rafi report the inverse shape (better at both extremes, worse in the middle). So if the balanced-accuracy run holds up, our result is not merely 'different', it is directionally opposite for the non-memorizers and flatly monotone for the memorizer. That is worth stating carefully rather than blurring into 'we also see non-monotonicity'. Caveat to carry: on human_enhancers_ensembl the two intermediate bins hold only 311 and 115 sequences (mid_band_frac 0.0138) against 18,770 and 11,774 at the extremes, so any middle-of-curve claim on THAT dataset rests on 426 of 30,970 test sequences. human_nontata_promoters has mid_band_frac 0.2243 and is the defensible dataset for the claim.

Scope note: the ledger entry D061 (decisions.jsonl:62) that records A1 also flags four additional stale citations (zhou2023dnabert2 is published at ICLR 2024 but cited as a 2023 arXiv preprint; bushuiev2024 is an ICLR 2024 GEM workshop paper; kovtun2024 has a bioRxiv v2; huang2025 has an arXiv v2 and is publicly contested as a stereoisomer artifact). Those are outside A1 as written but sit in the same bib file and will be touched by the same edit -- worth a separate item rather than silent inclusion. Both D061 and D062 are still status "open".

Finally, main.tex (the stale 20-page version) also cites rafi2025 at lines 62 and 78, as does main_v1_original.tex at 73 and 92. Per the brief those are superseded and need no fix; note only that main.tex:62 carries an explicit 'Two claims must be kept separate, and only the second is ours' concession paragraph that main_resubmission.tex condenses into the single clause at line 172. That condensation is what dropped the room for a graded-memorization concession, which is why the qualification at line 192 is now needed.

## A3 — PARTIAL (heavy-compute)

**Remaining:**
- Re-run the second dataset so the arm covers both leaky datasets, not one: `cd /Users/rikhinkavuru/homology_audit && ./venv/bin/python audit/experiments/exp_detector_specificity.py human_enhancers_ensembl`. Do NOT launch it via `nohup ... &` from a transient shell — audit/core/resources.py:96 reaps it the moment the parent zsh exits (that is exactly how the 01:19 attempt died after 1 second). Use run_in_background:true, or setsid/disown, or run it in the foreground with a long timeout. Budget ~15-40 min: stage 1 is 3,836,315,840 pairs (30,970 test x 123,872 train, ~16x the nontata run) and stage 2 is ~4,300 Smith-Waterman alignments.
- Before that re-run, note the script OVERWRITES results/detector_specificity_pairs.csv and detector_specificity_bands.csv with only the datasets passed on argv (exp_detector_specificity.py:285-286 concatenates only `allp`, which holds just this invocation's datasets). The human_nontata_promoters results are already backed up at results/detector_specificity_pairs.nontata.csv and results/detector_specificity_bands.nontata.csv — after the ensembl run, concatenate the ensembl output with those two backups so pairs/bands contain BOTH datasets.
- Generate the missing summary: results/detector_specificity_measured.csv does not exist. After merging, call summarise(pairs, pop) from exp_detector_specificity.py on the combined frames and write it, so the per-band and pooled precision/recall/FPR table exists as a file the manuscript can cite.
- Extend the script (or add a post-processing step) to emit recall and FPR AT EACH THRESHOLD, which is what the item asks for. summarise() currently pools at the single flag threshold FLAG=0.7 only (exp_detector_specificity.py:52, 245-275). Sweep the band edges 0.1/0.3/0.5/0.7/0.8/0.9 as thresholds, treating bands at or above each edge as flagged, and write a tidy per-threshold CSV (columns: dataset, ground_truth, threshold, recall, recall_lo, fpr, fpr_hi, precision, precision_lo). The nontata half of that table is already computable and is quoted in the evidence above.
- Rewrite paper/main_resubmission.tex:249. Delete the sentence "Specificity cannot be settled by pairwise sampling---the census statistic is a maximum over the training set, so bounding the per-pair rate for a $47{,}356$-row training set would need on the order of $10^{7}$ pairs---" and replace it with the measured per-pair operating characteristic: precision at Jaccard 0.7 is 1.000 (CP lower bound 0.978) and FPR is 0.000 (upper bound ~1e-6), against Smith-Waterman ground truth on 4,300 Horvitz-Thompson-weighted pairs. Keep the construction-and-provenance floor argument as corroboration rather than as the sole basis.
- Rewrite paper/supplementary.tex:373-384 (\S sec:s-detector). Replace "Specificity cannot be settled by pairwise sampling ... We therefore argue the estimator floor from construction and provenance, as the main text does" with the measured table, and add the per-threshold recall/FPR table as a new supplementary table alongside the existing Table~\ref{tab:sens}.
- State the honest limitation that the data actually shows, rather than dropping the 10^7 argument entirely: the recall LOWER bound remains weak. The sub-0.1 band holds 244,697,943 of 244,794,298 pairs (99.96%) and only 3,000 were adjudicated; 0/3000 homologs gives a Clopper-Pearson upper bound of 0.001229, admitting up to ~300,733 undetected homologs against an estimated 11,908 true positives, so recall_lo at threshold 0.7 is only 0.034 where the point estimate is 0.260. The correct revised framing is: the 10^7 argument survives for the recall lower bound but NOT for precision or FPR, both of which are now measured.
- Report that recall is strongly ground-truth-dependent and say so explicitly, since a reviewer will check: at threshold 0.7 recall is 0.875 under the strict criterion (id>=90%, cov>=80%), 0.260 under the primary (80%/50%), and 0.155 under the loose (70%/30%). Do not quote a single recall number without its criterion.
- Cite hashFrag \citep{rafi2025} as the template being adopted at the point where the new validation is introduced (main_resubmission.tex ~:249 and supplementary.tex ~:373), and add the corresponding item to paper/response_to_reviewers.md, which currently has no A3 response at all (its only 'recall' hits at lines 302-305 are the unrelated minority-recall imbalance panel).
- Append a decision to audit_recovered/decisions.jsonl recording that the detector's per-pair operating characteristic was measured against Smith-Waterman ground truth and that the 'cannot be settled by pairwise sampling' claim was retracted and narrowed to the recall lower bound. No entry currently exists.
- Side finding worth fixing while editing that sentence: main_resubmission.tex:249 is grammatically broken — the parenthetical em-dash is never closed, running "...on the order of $10^{7}$ pairs---The floor therefore rests..." with a capital T mid-sentence. Also verify the '$47{,}356$-row training set' figure: expkit loads human_enhancers_ensembl with 123,872 training rows and human_nontata_promoters with 27,097, so 47,356 matches neither leaky dataset.

**Notes:** The single most important fact: the manuscript has NOT been touched for this item at all, and still asserts the exact opposite in two places (main_resubmission.tex:249, supplementary.tex:373-378). This is not a citation gap — it is a live contradiction between a claim in the bundle and an experiment already sitting in results/.

Second: the experiment is genuinely half-run, and the half that is missing was killed by infrastructure, not by a design decision. audit/core/resources.py (untracked, new) terminates orphaned children when the parent shell exits, and it killed the human_enhancers_ensembl launch one second in at 01:19 today. Whoever relaunches must avoid a detached nohup from a transient shell or it will die the same way. The nontata results were manually backed up to *.nontata.csv immediately before that launch, which strongly suggests the operator knew the script overwrites pairs/bands with only the argv datasets — that merge step is still outstanding and is easy to lose.

Third, a contradiction the main thread should decide on deliberately: the measured numbers only PARTLY refute the manuscript. Precision (1.000, CP lower bound 0.978) and FPR (0.000, upper bound ~1e-6) are cleanly measured and the paper's refusal is simply wrong about them. But the recall lower bound genuinely is dominated by the sub-0.1 band exactly as the 10^7 argument predicted: 0/3000 adjudicated there yields a CP upper bound of 0.001229, i.e. up to ~300,733 possible missed homologs against ~11,908 estimated true positives, collapsing recall_lo to 0.034 against a 0.260 point estimate. Deleting the 10^7 argument wholesale would overcorrect and hand a reviewer an easy hit. Narrow it to the recall lower bound instead.

Fourth: recall at threshold 0.7 swings from 0.155 to 0.875 purely on the choice of homology criterion (loose 70/30 vs strict 90/80). Any headline recall number must ship with its criterion attached, and the three-cutoff sensitivity arm is already computed in the CSV, so this costs nothing to report honestly.

Fifth: supplementary.tex:383-384's existing \"per-pair false-positive rate at threshold 0.7 is 0.000 on all three clean real-DNA panels\" is easy to mistake for this item being already done. It is not — estimator_specificity_real.csv has no alignment ground truth (it presumes the clean datasets contain no homologs, which is the assumption under test) and reports no recall. It cannot substitute for the hashFrag Fig 2B-C template.

No ledger entry exists for this item, and audit/tools/check_numbers.py does not track any of these numbers, so nothing will catch a stale value here automatically once the text is written.

## A5 — PARTIAL (text-only)

**Remaining:**
- In paper/main_resubmission.tex, in the Datasets subsection (\subsection{Datasets}\label{sec:m-data}, line 59-60), add the upstream release pinning from results/reference_provenance.csv: state that human_nontata_promoters and human_enhancers_cohn derive from Ensembl release 97 GRCh38 toplevel DNA FASTA, and human_enhancers_ensembl and human_ocr_ensembl from the Ensembl release 100 Regulatory Build external-feature table (regulation_mart_100) plus the release-100 GRCh38 primary-assembly FASTA. Attribute these to the curators' own create_datasets.ipynb notebooks in ML-Bioinfo-CEITEC/genomic_benchmarks (the 'evidence' column of the CSV), not to our inference.
- In paper/main_resubmission.tex:112, change 'from the Regulatory Build's merged ``MultiCell'' segmentation \citep{zerbino2015regbuild}' to name the release explicitly, e.g. 'from the release-100 Regulatory Build's merged ``MultiCell'' segmentation'.
- In paper/main_resubmission.tex:65 (Sec 2.2), after 'via the UCSC REST API', add the pinned assembly version and access date: hg38 = GRCh38 Genome Reference Consortium Human Reference 38, accession GCA_000001405.15 (UCSC 'Dec. 2013' assembly); hg19 = GRCh37, accession GCA_000001405.1 (UCSC 'Feb. 2009'); queried via https://api.genome.ucsc.edu/getData/sequence, accessed 2026-07-20. All four values are in results/reference_provenance.csv rows 6-7.
- In paper/main_resubmission.tex Data Availability (line 274-275), add archive URLs so the upstream artifacts stay resolvable: https://jul2019.archive.ensembl.org for release 97 and https://apr2020.archive.ensembl.org for release 100, with access date 2026-07-20, and state that all Ensembl/UCSC URLs were confirmed resolvable (HTTP 200) on that date per results/reference_provenance.csv.
- Add the forward-compatibility note the reviewer asked for (it exists NOWHERE in the repo - not in the script, CSV, or text): state that Ensembl release 116 (June 2026) is the final release served on the current platform, with data moving to a new site in summer 2026, which is why archive-host URLs (jul2019/apr2020.archive.ensembl.org) rather than the rolling ftp.ensembl.org paths are given as the citable form. Put this in Data Availability next to the archive URLs.
- Add a database-resource entry to paper/references.bib for the Ensembl Regulatory Build release 100 and/or the UCSC Genome Browser assembly hub, carrying version + access date fields (the existing zerbino2015regbuild at references.bib:144-149 is the 2015 method paper and cannot carry a release/access date). Cite it at the new main_resubmission.tex:59-60 and :112 text.
- Optionally extend audit/experiments/exp_reference_provenance.py's ARCHIVE dict (line 31) with release 116 -> its archive host and re-run, so the release-116 claim is machine-verified like the others rather than asserted in prose. Re-run: PYTHONPATH=. ./venv/bin/python audit/experiments/exp_reference_provenance.py
- Add a line to paper/response_to_reviewers.md answering A5, quoting the pinned releases (97 and 100), the UCSC accessions (GCA_000001405.15 / GCA_000001405.1), the access date (2026-07-20), and pointing to results/reference_provenance.csv as the dated machine-verified record.
- Append an A5 decision entry to audit_recovered/decisions.jsonl recording that the provenance experiment ran on 2026-07-20 with all URLs HTTP 200 and citing results/reference_provenance.csv - no A5 entry currently exists.

**Notes:** Clean split: the EXPERIMENT side of A5 is fully done and is genuinely high quality (live HTTP verification, not transcription, all 200s, dated 2026-07-20), but the MANUSCRIPT side is essentially untouched. Not a single one of the pinned values (release 97, release 100, GCA_000001405.15, GCA_000001405.1, jul2019/apr2020 archive hosts, access date) appears anywhere in main_resubmission.tex, supplementary.tex, references.bib, response_to_reviewers.md, or cover_letter.md. This is the classic 'CSV exists but text does not cite it' failure mode, so no re-running is needed - effort is pure prose insertion from an existing CSV.

One clause of the item IS already satisfied: the reviewer's statement that 'Section 2.2 verification used the UCSC REST API against GRCh38' is confirmed accurate at main_resubmission.tex:65, and n=49 there matches the 49 data rows in results/assembly_verification.csv. So that sentence needs augmenting with version/date, not rewriting.

Two gaps go beyond 'text does not cite CSV' and need new content: (a) the Ensembl release 116 / June 2026 platform-migration note is absent from the entire repository including the experiment script (ARCHIVE dict at exp_reference_provenance.py:31 has only keys 97 and 100), so it must be written from the reviewer's own supplied fact; (b) the item asks for a 'UCSC assembly hub version' - the CSV supplies the assembly accession and UCSC's date-string description ('Dec. 2013 (GRCh38/hg38)') but nothing that is literally a hub version, so decide whether the accession satisfies this or whether a genuine hub/API version needs fetching.

NOT superseded and NOT fixed-in-stale-file: paper/main.tex (the older 20pp version) also lacks all provenance values, so there is no gap between the two files on this item - both are missing it equally.

Also worth flagging to the main thread: the archive-host URLs in the script (ARCHIVE dict) are hardcoded from Ensembl's naming convention and are the only two fields in reference_provenance.csv that are NOT network-verified (the code HEAD-checks the ftp.ensembl.org artifact URLs, not the archive hosts). If the archive URLs go into the paper as the citable form, they should be resolvability-checked first.

## B1 — PARTIAL (cheap-compute)

**Remaining:**
- Run the third arm, which is already written and never executed: `cd /Users/rikhinkavuru/homology_audit && PYTHONPATH=. ./venv/bin/python -m audit.experiments.exp_eval_side_inflation 2>&1 | tee exp_eval_side_inflation.log`. Confirm results/eval_side_inflation.csv is created with 8 rows (4 models x 2 datasets) and non-empty eval_side_ci_lo/eval_side_ci_hi.
- Sanity-check the produced CSV against the values already derivable from results/extended_models_long.csv before using it: for human_enhancers_ensembl RF expect acc_asshipped_full~0.8596, acc_asshipped_novelonly~0.7698, acc_corrected_resplit~0.6957, eval_side_inflation~0.090, split_side_effect~0.074. For LinearSVC expect acc_asshipped_full~0.7980, acc_asshipped_novelonly~0.782, acc_corrected_resplit~0.7952, eval_side_inflation~0.016, split_side_effect~-0.013 (NEGATIVE — corrected accuracy is slightly HIGHER than novel-only). If the run disagrees materially with these, stop and reconcile rather than overwriting the frozen numbers.
- Fix the false decomposition at paper/main_resubmission.tex:175. The sentence 'The fall decomposes, and only one part is the split: ... but by only $1.2$ points. The remaining $10$ points is a retraining penalty' presents two RANKING MARGINS (novel margin 1.2 pts; corrected margin 10.0 pts) as if they decomposed the 16.5-point fall. 1.2 + 10 = 11.2 != 16.5. Rewrite so the fall of the forest is decomposed as ~9.0 points evaluation-side (0.8596 -> 0.7698, same fit, no re-split, no retraining) + ~7.5 points split-side (0.7698 -> 0.6951, refit after near-duplicate partners are stripped from training), with the cluster-bootstrap CI on the eval-side term from the new CSV. Keep the 1.2-point novel margin, but label it explicitly as a ranking margin, not a component of the fall.
- Add BOTH numbers to the abstract at paper/main_resubmission.tex:34-39, which is the explicit B1 requirement and currently mentions neither (nor even 16.5). Insert into the \textbf{Results:} block a clause of the form: correcting the split costs the as-shipped best model 16.5 points on human_enhancers_ensembl, of which ~9 points is evaluation-side inflation measured on the novel stratum with no retraining and ~7.5 points is a split-side retraining penalty; the ranking changes on the evaluation-side arm alone (LinearSVC 0.782 > RF 0.770).
- Report the per-model eval-side inflation so the ~1-2 point expectation in B1 is visibly met and not silently contradicted. NOTE FOR THE MAIN THREAD: the reviewer's 'evaluation-side inflation ~1-2 points' matches the NON-memorizers (LinearSVC 0.798 -> 0.782 = 1.6 pts), NOT the memorizing forest (9.0 pts). Frame it as: eval-side inflation is confined to the memorizer (RF 9.0 pts) and is 1-2 points for the linear models — which strengthens the memorization mechanism rather than weakening it. Do not report a single dataset-level 'eval-side inflation' number that hides this spread.
- Cover human_nontata_promoters, currently absent from the decomposition entirely. The script already computes it; add a sentence to \S\ref{sec:r-rank} (main_resubmission.tex:174-176) giving its eval-side and split-side terms, and reconcile with results/novelonly_ranking.csv which shows rf_rank_novelonly=1 there (ranking does NOT change on the novel-only arm), consistent with the 'accuracy-only demotion / cautionary partial case' framing at main_resubmission.tex:236.
- Report the prevalence confound the script computes, so the eval-side number is not attacked as a class-composition artifact. Quote eval_side_inflation_balanced alongside the raw value and the constant_classifier_eval_side_gap bound; supplementary.tex:119 already states the novel stratum is 19.68% positive vs 99.97% in the >=0.9 stratum, so the raw gap is partly prevalence. Put this in supplementary.tex:204-206, which currently defines the novel-only arm but gives no numbers.
- Update paper/response_to_reviewers.md:241-249, which repeats the same 1.2/10 margin framing and claims 'the manuscript now separates them (§3.6)'. Replace with the true eval-side/split-side split and point at the new results/eval_side_inflation.csv plus the revised abstract.
- Add a gate to audit/tools/check_numbers.py binding the new abstract/Results numbers to results/eval_side_inflation.csv (it currently has no eval_side/novelonly assertion at all), then re-run the gate and paper/build.sh so main_resubmission.pdf is regenerated from the edited source.
- Append a decisions.jsonl entry for B1 to audit_recovered/decisions.jsonl — there is currently none (66 lines, only unrelated D065 matches). Record: the third arm was written but never run, the 1.2+10 vs 16.5 arithmetic defect, and the abstract addition.

**Notes:** CONTRADICTION FOUND, and it is the strongest fact here. main_resubmission.tex:175 opens with 'The fall decomposes, and only one part is the split' and then supplies $1.2$ and $10$ — but those are not components of the 16.5-point fall, they are the novel-stratum ranking margin (LinearSVC 0.782 - RF 0.7698 = 1.2) and the corrected ranking margin (LinearSVC 0.7952 - RF 0.6957 = 10.0). They sum to 11.2, not 16.5. The reviewer's B1 text ('Only 1.2 of 16.5 points is measurement on novel sequences; ~10 is a retraining penalty') is them reading this sentence at face value — i.e. the sentence actively misleads, and the reviewer's own framing of the item inherits the error. The real decomposition (9.0 eval-side + 7.5 split-side = 16.45) appears nowhere in the bundle. Whoever executes this should fix the arithmetic, not just add the abstract sentence.

SECOND HEADS-UP: B1 predicts 'evaluation-side inflation ~1-2 points'. That is TRUE for LinearSVC (0.798->0.782 = 1.6 pts) but FALSE for the forest, where it is 9.0 pts. Running the experiment will therefore produce a headline eval-side number ~5x the reviewer's stated expectation. This is not a problem to hide — it is the memorization mechanism showing up exactly where the paper predicts (confined to the memorizer) — but the rebuttal must address the discrepancy head-on rather than quoting only the 1.6 that matches, which would be selective quotation of the kind commit cbeb235 already gated against.

THIRD: B1 also predicts 'the ranking still changes' on the third arm. results/novelonly_ranking.csv already confirms this for human_enhancers_ensembl (rf_rank_leaky=1 -> rf_rank_novelonly=2, LinearSVC best at 0.782) but REFUTES it for human_nontata_promoters (rf_rank_novelonly=1, unchanged). The nontata case must be conceded, consistent with the existing 'cautionary partial case' framing at main_resubmission.tex:236 — do not let the third arm be written up as if it reproduced on both datasets.

The good news: exp_eval_side_inflation.py is a careful, well-specified script (paired cluster bootstrap on the gap, balanced-accuracy variant, constant-classifier bound, corrected column READ from frozen extended_models_long.csv rather than recomputed so it cannot drift), all its dependencies resolve, and it covers both datasets and all four models. This item is roughly one script run plus a focused edit to main_resubmission.tex:175 and the abstract — the intellectual work is already done, it simply was never executed and never reached the manuscript.

## B2 — PARTIAL (cheap-compute)

**Remaining:**
- Wait for PID 98049 (exp_detector_specificity.py human_enhancers_ensembl) to finish; poll exp_detector_specificity_ensembl.log until it prints the summary table. Expect ~5-15 min from 1:20AM based on the nontata run taking 00:36:22->00:39:30.
- IMMEDIATELY AFTER IT FINISHES, merge and do not lose the nontata arm: the job overwrites results/detector_specificity_pairs.csv and _bands.csv with ensembl-only rows. Run: pd.concat([read('results/detector_specificity_pairs.nontata.csv'), read(ensembl-only pairs)]) and the same for _bands, write both back to the un-suffixed filenames.
- Regenerate results/detector_specificity_measured.csv over BOTH datasets by calling summarise(merged_pairs, merged_pop) from audit/experiments/exp_detector_specificity.py (import the module and call summarise directly -- do NOT re-run __main__, which would recompute and re-overwrite). Verify the output has POOLED rows for both human_nontata_promoters and human_enhancers_ensembl at all three ground_truth cutoffs.
- Rewrite paper/main_resubmission.tex:249. Delete the clause 'Specificity cannot be settled by pairwise sampling---the census statistic is a maximum over the training set, so bounding the per-pair rate for a $47{,}356$-row training set would need on the order of $10^{7}$ pairs---The floor therefore rests on construction and provenance:' and replace with the measured result: per-pair precision 1.000 (lower bound 0.978) and FPR 0.000 (upper bound 1e-6) at Jaccard 0.7, recall 0.260, from 4,300 Smith-Waterman-adjudicated pairs stratified over a full enumeration of 244,794,298 test-train pairs (plus the ensembl figures once merged). Keep the construction/provenance readings (0.012, 0.004, 0.001-0.016 vs 0.384/0.406) but demote them explicitly to corroboration, e.g. 'Provenance corroborates: ...'. Also fixes the '---The' typo.
- Rewrite paper/supplementary.tex:373-384. Replace the opening 'Specificity cannot be settled by pairwise sampling. The census statistic is not a per-pair score: ... would take on the order of $10^{7}$ pairs. We therefore argue the estimator floor from construction and provenance, as the main text does.' with the measured operating characteristic. KEEP the genuinely surviving caveat that the census statistic is a maximum over the training set so per-pair rates must be pushed through n_train -- exp_detector_specificity.py:10-12 explicitly says that caveat survives and is separate. Retain the heavy-tail observation at :378-382 (Jaccard 0.26 on real DNA vs 0.019 on uniform random). Replace the unsourced ':383-384 measured per-pair false-positive rate at threshold $0.7$ is $0.000$' with the properly sourced FPR 0.000 [upper bound 1.0e-6] from detector_specificity_measured.csv.
- Add a per-band table to paper/supplementary.tex in hashFrag Fig-2C format (bands as rows; columns n_sampled, band population, fraction homolog, Clopper-Pearson 95% CI, and the derived precision/recall/FPR). Use: [0.7,0.8) 167/167=1.000 pop 4,291; [0.8,0.9) 167/167=1.000 pop 4,188; [0.9,1.0] 166/166=1.000 pop 3,429; [0.5,0.7) 200/200=1.000 pop 9,838; [0.3,0.5) 176/200=0.880 pop 12,044; [0.1,0.3) 86/400=0.215 pop 62,565; [0,0.1) 0/3000=0.000 [0,0.00123] pop 244,697,943 -- plus the ensembl columns once available. Cite \citep{rafi2025}, already in references.bib.
- Report the three ground-truth cutoffs as a sensitivity arm so the recall figure is not cherry-picked: recall 0.260 (primary, id>=0.80/cov>=0.50), 0.875 (strict, id>=0.90/cov>=0.80), 0.155 (loose, id>=0.70/cov>=0.30); precision 1.000 and FPR 0.000 under all three.
- Use the measured recall of 0.260 to quantify the existing qualitative claim at main_resubmission.tex:249 and :71 that 'every leak fraction is a lower bound' -- the detector misses ~74% of Smith-Waterman homologs at the primary cutoff, which is now a number rather than an assertion. Note the [0.5,0.7) band is 200/200 homologous, i.e. 9,838 true homolog pairs sit just under the flag threshold.
- Trace or fix the '$47{,}356$-row training set' figure at main_resubmission.tex:249 and supplementary.tex:376. It matches neither leaky dataset (nontata train = 27,097; ensembl train ~= 123,872). If the sentence is rewritten as instructed above the number disappears from main_resubmission.tex, but check whether it survives anywhere else, including paper/main.tex:373.
- Add a check_numbers.py gate for the new figures (precision 1.000/0.978, recall 0.260, FPR 0.000/1e-6, and the seven per-band counts) reading results/detector_specificity_measured.csv, matching the existing pattern in audit/tools/check_numbers.py.
- Add a paragraph to paper/response_to_reviewers.md answering this item explicitly -- it currently has no response to it (grep for 'specific|adjudicat|Smith|BLAST|10^7' returns only unrelated hits at :302-305, :430, :490, :670). State that the reviewer was correct that enumeration is cheap, that the ~1e7 argument has been withdrawn, and give the measured precision/recall/FPR.
- Append a decision to audit_recovered/decisions.jsonl recording the withdrawal of the 1e7 unmeasurability argument, with evidence results/detector_specificity_measured.csv, detector_specificity_pairs.csv, detector_specificity_bands.csv and audit/experiments/exp_detector_specificity.py. No entry currently exists for this item.
- Rebuild paper/main_resubmission.pdf and paper/supplementary.pdf via paper/build.sh after the text surgery, and re-run the numeric gate.

**Notes:** PARTIAL, and closer to done than it looks -- but the manuscript side is untouched, so as the bundle stands today the reviewer's objection is fully live.

The single most urgent thing: a job is RUNNING RIGHT NOW (PID 98049, human_enhancers_ensembl) and it WILL destroy the completed nontata results in results/detector_specificity_pairs.csv and _bands.csv, because exp_detector_specificity.py:285-286 concatenates only the datasets passed on argv. Someone anticipated this and made .nontata.csv backups at 01:19. Do not skip the merge step, and do not re-run __main__ afterward to 'regenerate' the summary -- that would overwrite again. Import summarise() and call it on merged frames.

The reviewer is straightforwardly right on the substance, and the repo already proves it: results/detector_specificity_bands.csv contains a completed full enumeration of all 244,794,298 test-train pairs, which is exactly the Section 2.4 sparse matrix product the reviewer points to. Only 4,300 Smith-Waterman adjudications were needed. The '~1e7 pairs' figure in the manuscript conflates enumeration with adjudication and should be withdrawn rather than defended.

The measured result is good news for the paper and should not be buried: precision 1.000 (CP lower bound 0.978) and FPR 0.000 (upper bound 1e-6) mean the detector produces essentially no false flags, which is a stronger and cleaner statement than the provenance floor argument it replaces. Recall 0.260 is also useful -- it converts the paper's existing qualitative 'every leak fraction is a lower bound' hedge (main_resubmission.tex:71, :249) into a measured quantity, and shows the [0.5,0.7) band holds 9,838 pairs that are 200/200 true homologs sitting just below the flag. Report the strict/loose arms (recall 0.875 / 0.155) so the 0.260 does not look selected.

One caveat genuinely survives and must be kept, not deleted with the rest: the census statistic is a maximum over the training set, so a per-pair rate has to be pushed through n_train before it bounds the per-test-sequence fraction. The experiment's own docstring (:10-12) flags this as separate and surviving. Keeping it is what makes the rewrite honest rather than a swap of one overclaim for another.

Two smaller defects found in the same sentence, both worth fixing while it is being rewritten: the '$47{,}356$-row training set' matches neither leaky dataset (27,097 and ~123,872) and I could not trace it to anything in results/leakage_report_card.csv; and main_resubmission.tex:249 has a broken em-dash construction, 'pairs---The floor therefore rests', where the closing dash should be a period.

Also note supplementary.tex:383-384 already asserts 'the measured per-pair false-positive rate at threshold $0.7$ is $0.000$ on all three clean real-DNA panels' with no cited source. The new CSVs finally substantiate an FPR claim the supplement was already making, which is an argument for citing them rather than leaving them orphaned.

## B3 — PARTIAL (heavy-compute)

**Remaining:**
- Run the clean-control arm for the MISSING dataset: `python -m audit.experiments.exp_spread_control --arm roster --datasets demo_human_or_worm --cap 20000`. Data and cluster caches already exist (datacache/demo_human_or_worm__cap20000__seed0.pkl, datacache/comp_demo_human_or_worm_thr0.7_k8_jaccard.npy), so this is 9 model fits × 2 splits at n=20,000. Verify completion by checking that datacache/spread_control/corr_demo_human_or_worm_n20000.npz appears and that the new rows carry non-empty spread_ci_lo/spread_ci_hi.
- Re-run cohn through the REAL roster arm rather than the derive shortcut, so the top-3 spreads carry cluster-bootstrap intervals: `python -m audit.experiments.exp_spread_control --arm roster --datasets human_enhancers_cohn --cap 20000`. Without CIs a spread difference of 0.0218 vs 0.0210 cannot be called 'does not expand' — that is a point-estimate difference of 0.0008 with no uncertainty attached, and a reviewer will say so. Confirm datacache/spread_control/corr_human_enhancers_cohn_n20000.npz is written.
- BEFORE running the difficulty-matched arm, settle the target operating point. The script defaults to `--target-acc 0.6951`, documented at exp_spread_control.py:319 as 'RF corrected on ensembl'. But the item asks to match the CORRECTED SPLIT ACCURACY LEVEL OF THE TRIO whose spread is being measured, which is mean_acc=0.6444 (results/spread_control.csv, row 'human_enhancers_ensembl / corrected_split/as_shipped_top3'). Using 0.6951 matches the wrong quantity and makes the control weaker than asked. Run with `--target-acc 0.6444`, and consider reporting 0.6951 as a secondary sensitivity row.
- Run the difficulty-matched control: `python -m audit.experiments.exp_spread_control --arm matched --target-acc 0.6444`. This is the expensive step — it fits 6 models (LR, LinearSVC, GaussianNB, then MLP, kNN-1, ExtraTrees) at FULL scale n=154,842 on human_enhancers_ensembl (exp_spread_control.py:149-169); kNN-1 and MLP at that scale dominate the cost. The function checkpoints per model to datacache/spread_control/corr_ensembl_{m}.npy, so it is safely resumable — verify all 6 .npy files exist before trusting the output row.
- Fix the docstring/code mismatch in exp_spread_control.py before running the matched arm: line 33 documents the held-out difficulty panel as '(LR, LinearSVC, HGB, GaussianNB)' — four models — but line 146 defines `DIFF_PANEL = ["LR", "LinearSVC", "GaussianNB"]` — three. This changes the number of difficulty bands from 5 to 4 and thus the granularity of the re-weighting. Decide which is intended, make code and docstring agree, and record the choice, since the panel definition is the load-bearing 'never from the three models being measured' guarantee.
- When the matched arm completes, apply the item's decision rule literally. Read `top3_spread` and its CI on the row experiment='difficulty_matched'. If the difficulty-matched spread at the corrected operating point is close to 0.2314 (i.e. the 23 points reproduce WITHOUT de-leaking), the expansion is a ceiling artifact and you MUST delete 'it is blind to enormous real differences between models it ranks as equals' from paper/main_resubmission.tex:177 AND from paper/main.tex:226. If it stays near the as-shipped 0.0015-0.03 range, the claim survives and must be defended with the number in-text.
- Also inspect the leak-fraction diagnostic the matched arm prints and stores in its note (exp_spread_control.py:224-230, 258-260): it reports the sim>=0.9 leak fraction inside the difficulty-matched draw against the full as-shipped test set. If the difficulty tilt has substantially re-derived the novel-only stratum (matched leak fraction far below full-test leak fraction), the control is partly circular and that limitation must be stated in the text, not buried in the CSV note.
- Write the surviving numbers into paper/main_resubmission.tex:177 (and mirror into paper/supplementary.tex). At minimum the paragraph must state: (a) the clean-control top-3 spread on human_enhancers_cohn and demo_human_or_worm, as-shipped vs corrected, with intervals, establishing that a whole-cluster re-split alone does not manufacture spread; (b) the difficulty-matched spread at the corrected operating point, establishing that the expansion is not a ceiling artifact. Currently the manuscript asserts the 23-point conclusion with no spread control of any kind.
- Carry over the existing n=20,000 subsample disclosure to the new spread claims. main_resubmission.tex:60 and the Table caption at main_resubmission.tex:101 already disclose that cohn and worm deltas are 20k subsamples, and main_resubmission.tex:189 repeats it for P4. The new cohn/worm SPREAD statistics inherit the same caveat and the disclosure sentences must be extended to cover spread, not just drops and leak fractions.
- Add a ledger entry to audit_recovered/decisions.jsonl recording this item, the arms run, the target-acc choice, and the delete-or-keep verdict on 'blind to enormous real differences'. No entry currently exists (the two "node":"B3" lines D061/D062 are an unrelated bibliography-currency numbering).
- Consider adding results/spread_control.csv to the audit/tools/check_numbers.py gate so the in-text spread numbers are verified against the CSV, matching how other numbers in the bundle are gated. It is currently ungated (grep 'spread' in run_audit.py returns nothing; the check_numbers.py:745 hit is an unrelated tuning-sweep variable).

**Notes:** HEADLINE: the experiment script is good and was clearly written for exactly this item, but ONLY its no-compute `derive` arm ran. Both real arms — the clean-dataset roster re-split and the difficulty-matched control — never executed. The empty datacache/spread_control/ directory is the decisive proof: both real arms unconditionally write checkpoint artifacts there (exp_spread_control.py:138 and :166) and neither exists. This is a textbook case of the trap the task warned about: a script exists, a CSV exists, and neither is evidence the experiment ran.

WHAT THE DERIVE ARM IS: a pure re-read of results/roster_rankings.csv into a spread table. I verified this by matching cohn values field-by-field (MLP 0.7382/0.7334, LR 0.7362/0.7378, HGB 0.7164/0.7168). Zero new fitting. Its only real contribution is arranging pre-existing accuracies into top-3 spreads.

THE ONE PIECE OF GOOD NEWS: the derived cohn numbers point the right way. Clean dataset, as-shipped top-3 spread 0.0218 → same trio on the corrected split 0.0210. Spread does not expand when a whole-cluster re-split is applied to a clean dataset. That is genuinely the clean control the item asks for, for one of the two named datasets — but as point estimates only, with no interval, so 'does not expand' currently rests on a 0.0008 difference with no uncertainty attached.

WHY THE COHN CONTROL DOES NOT DISCHARGE THE ITEM EVEN IF ACCEPTED: cohn's operating point is ~0.73, nowhere near the ~0.87 ceiling that human_enhancers_ensembl sits at as shipped. The reviewer's objection is specifically that spread expands MECHANICALLY off a ceiling. A control run at 0.73 does not exercise the ceiling mechanism at all. The difficulty-matched arm is the only one that tests the actual objection, and it is precisely the arm that did not run. Do not let the cohn result be used to close this item.

CONTRADICTION TO FLAG: the script's own default `--target-acc 0.6951` ('RF corrected on ensembl', line 319) does not match what the item asks for. The item says match the corrected-split accuracy level of the trio under test, which is 0.6444 per the CSV's own corrected_split/as_shipped_top3 row. Running the default would match the wrong operating point and produce a weaker control than requested. This needs a human decision before compute is spent, because the matched arm is expensive (6 full-scale fits at n=154,842) and you do not want to run it twice.

COST REALITY: the worm roster arm is cheap and fully cached — run it first, it may be an hour. The matched arm is the expensive one (kNN-1 and MLP at n=154,842 dominate) but it checkpoints per model, so it is resumable; verify all 6 corr_ensembl_*.npy land before trusting the output.

SCOPE HONESTY: the manuscript side is untouched — zero traces in main_resubmission.tex, supplementary.tex, main.tex, response_to_reviewers.md, or cover_letter.md. Even if every arm had run, this item would still be PARTIAL because nothing reached the text. The 'blind to enormous real differences' sentence stands unhedged in both the current and the stale manuscript, and no ceiling caveat appears anywhere in main_resubmission.tex.

MINOR CODE DEFECT worth fixing before the expensive run: exp_spread_control.py:33 documents a 4-model difficulty panel, line 146 implements a 3-model one. That changes band granularity from 5 bands to 4, and the panel's composition is the load-bearing guarantee that the difficulty axis is independent of the trio being measured — so the mismatch should be resolved deliberately, not silently.

NOT SUPERSEDED: I looked for a reason this could be moot and found the opposite — the claim is live, prominent, load-bearing for the paper's central 'benchmarks are blind' message, and repeated identically in both manuscript versions.

## C10 — PARTIAL (cheap-compute)

**Remaining:**
- Write /Users/rikhinkavuru/homology_audit/audit/experiments/exp_datacache_manifest.py. For each of the 9 (dataset, cap, seed) combos in audit/tools/prefetch.py COMBOS, load datacache/{dset}__cap{c}__seed{seed}.pkl and emit one row to results/datacache_manifest.csv with columns: dataset, cap, seed, pkl_filename, sha256_pkl (SHA-256 of the raw pickle bytes), sha256_content (SHA-256 over canonicalized content — join sorted (id, seq, label, split) tuples with a fixed separator and encode UTF-8; this is the release-independent digest and is the scientifically load-bearing one, since pickle bytes shift with pandas/protocol versions), n_rows, n_train, n_test, class_counts, genomic_benchmarks_version (read from importlib.metadata.version('genomic_benchmarks')), pandas_version, extraction_date (ISO date from the pkl file mtime, os.path.getmtime).
- Run it: cd /Users/rikhinkavuru/homology_audit && PYTHONPATH=. ./venv/bin/python audit/experiments/exp_datacache_manifest.py — confirm results/datacache_manifest.csv has 9 rows and non-empty digests.
- Edit paper/supplementary.tex: insert after line 247 ("The pinned dependency list ships as \texttt{results/requirements.txt}.") inside \subsection{Software environment} (sec:s-env) a new paragraph naming the extraction date (from the manifest, expected 2026-07-18/19/20 depending on pkl mtimes), stating that SHA-256 digests of all nine cached extractions ship as \texttt{results/datacache\_manifest.csv}, and quoting the two leaky datasets' sha256_content digests inline (truncated to 16 hex chars is acceptable and conventional) so a reader can verify without cloning.
- Edit paper/supplementary.tex sec:s-env in the same paragraph: state the UPSTREAM reference builds from results/reference_provenance.csv verbatim — GRCh38 / Ensembl release 97 for human_nontata_promoters and human_enhancers_cohn; GRCh38 / Ensembl release 100 for human_enhancers_ensembl and human_ocr_ensembl — noting these were recovered from the curators' own create_datasets.ipynb notebooks and re-verified at HTTP 200 on 2026-07-20 against jul2019/apr2020.archive.ensembl.org.
- Edit paper/main_resubmission.tex:91: replace the trailing clause "Because \texttt{genomic\_benchmarks} $1.0.0$ re-extracts on every call we cache locally; audits against a different release should re-derive the report card." with a version that pins the bytes and forwards to the supplement, e.g. "Because \texttt{genomic\_benchmarks} $1.0.0$ re-extracts on every call we cache locally once and read from that cache thereafter; SHA-256 digests of every cached extraction, with the package version, upstream reference build and extraction date, are in Supplementary \S\ref{sec:s-env}, and audits against a different release should re-derive the report card." Keep it to one sentence — §2.6 is already the densest paragraph in the Methods and the article is page-limited.
- Add a check to audit/tools/check_numbers.py following the existing csv()-loader pattern at :213-370: load datacache_manifest.csv and assert that every sha256_content digest quoted in paper/supplementary.tex matches the CSV, so the digests cannot drift from the manuscript the way the Ensembl release numbers already have.
- Optional but recommended (fixes the live defect, not just the disclosure): change the cache key in audit/core/expkit.py:53 from f"{dset}__cap{c}__seed{seed}.pkl" to include the genomic_benchmarks version, so a different release cannot silently reuse a stale pickle. Note this invalidates the existing datacache and forces a re-prefetch, so gate it behind author approval before running.
- Optional: add a check_numbers.py check covering the ungated GRCh38 claims at main_resubmission.tex:65 (49/49, 915, 0.48%, 190,973, 1 of 48) against results/assembly_verification.csv — currently no numeric gate touches that CSV.

**Notes:** Scored 1.5 of C10's 4 asks. Package version: DONE. Reference build: DONE for our own GRCh38 verification (main_resubmission.tex:65, real and empirically backed), NOT DONE for the upstream Ensembl releases. SHA-256 checksums: zero trace anywhere. Extraction date: zero trace in the bundle.

THE HEADLINE FINDING for the main thread: results/reference_provenance.csv is a fully-run experiment with real, network-verified numbers (Ensembl releases 97 and 100, HTTP 200, dated 2026-07-20) whose output appears NOWHERE in main_resubmission.tex or supplementary.tex. This is the textbook PARTIAL shape the task warned about — script ran, CSV exists, manuscript never cites it. Roughly a third of C10 can be discharged by transcription alone, with no new compute.

METHODOLOGICAL WARNING on the fix: do not implement the checksum as SHA-256 of the .pkl files alone. Pickle bytes are not stable across pandas versions or pickle protocols, so a reader on pandas 3.1 would get a mismatch on byte-identical DATA and conclude the audit is irreproducible — strictly worse than the current silence. The digest that actually pins the audited bytes is one over canonicalized content (sorted (id, seq, label, split) tuples). Compute both, but the content digest is the one to quote in the manuscript.

LIVE DEFECT, not just a disclosure gap: audit/core/expkit.py:53 keys the cache on (dataset, cap, seed) only. The package version is absent from the key, so a reader who upgrades genomic_benchmarks and re-runs gets the OLD pickle back silently — exactly the contamination C10 anticipates. Fixing this invalidates the existing datacache and forces a full re-prefetch, so it needs author sign-off before execution; it is listed last in 'remaining' for that reason.

NOT SUPERSEDED, and I checked: the cited sentence survives verbatim in the current 8pp bundle at main_resubmission.tex:91, and the old main.tex:180 has no checksums either, so there is no version where this was ever addressed.

CONTEXT ON SCOPE: audit_recovered/HIGH_SEVERITY.md:82-83 recommends archiving to Zenodo with a DOI plus commit SHA. Do not treat that as covering C10 — it pins the CODE artifact; C10 pins the DATA bytes. They are orthogonal, and D042 records that the git push is currently HELD at the author's instruction, so the Zenodo route is blocked anyway while the data-pinning route is not.

SECONDARY: check_numbers.py covers neither assembly_verification.csv nor reference_provenance.csv, so the GRCh38 numbers at main_resubmission.tex:65 sit outside the numeric-consistency gate that protects the rest of the paper's claims. Worth closing while touching this area, but it is not part of C10 proper.

## C2 — PARTIAL (text-only)

**Remaining:**
- Create /Users/rikhinkavuru/homology_audit/PREREGISTRATION.md at repo root. It does not exist and never has. Structure it as: (a) a table of the two prereg documents with their registration timestamps and adding-commit hashes; (b) verbatim pasted `git log` output proving prediction-before-result for the GUE registration; (c) an explicit honest concession that the CNN/deep registration is same-commit and therefore weak.
- In PREREGISTRATION.md, paste the verbatim output of: `git log --diff-filter=A --format='%H %ad %s' --date=iso -- results/tier1_preregistration.md` (= 2edad5c 2026-07-18 17:00:21 -0400) and `git log --diff-filter=A --format='%H %ad %s' --date=iso -- results/gue_census.csv` (= 3af3eb4 2026-07-19 02:21:24 -0400), showing the 9h21m gap. Use FULL 40-char hashes, not abbreviated, so the claim is checkable.
- In PREREGISTRATION.md, state the CNN concession explicitly with its hash: results/deep_preregistration.md was added in commit 33b1825 (2026-07-18 14:05:15 -0400) in the SAME commit as the experiment code, so it carries no priority evidence — matching what paper/supplementary.tex:143-147 already says. Do NOT let the new file overstate what the manuscript concedes; ledger D065 shows this exact failure mode already occurred once.
- Add a 'Registered:' timestamp header and repo-state hash to results/deep_preregistration.md mirroring results/tier1_preregistration.md:3-4, OR add an explicit line stating it deliberately has none because it was same-commit. Currently the asymmetry between the two prereg docs is unexplained and looks like an oversight.
- Amend paper/main_resubmission.tex:275 (Data Availability) to cite the specific commit hash and/or a tagged release of the GitHub repo, and to name PREREGISTRATION.md as the file documenting registration provenance. Currently it is a bare URL with no version anchor.
- Amend paper/main_resubmission.tex:213 (§3.12, sec:r-gue) to attach a verifiable anchor to the claim 'recorded before that suite was examined' — e.g. add a clause naming the commit hash 2edad5c and pointing to PREREGISTRATION.md. This is the ONE registration whose priority git actually proves, and the manuscript currently asserts it with no evidence a reviewer can check.
- Optionally strengthen paper/main_resubmission.tex:78 (§2.5) by adding, after 'not an externally timestamped registration', a pointer such as 'commit hashes and timestamps are published in PREREGISTRATION.md in the code repository'. Keep the existing disclaimer sentence verbatim — it is exactly what the reviewer asked for and must not be weakened.
- Decide and record a decision on the OSF deposit (explicitly optional in the item). If declining, add one line to PREREGISTRATION.md saying so and why, so the reviewer sees it was considered rather than ignored.
- Append a decision entry to audit_recovered/decisions.jsonl recording the C2 resolution; no existing line covers the hash-publication half of this item.

**Notes:** Cleanly split item: the 'state plainly in Section 2.5' sub-requirement is fully DONE and arguably exemplary (main_resubmission.tex:78 plus a more self-critical supplementary.tex:143-147); the 'publish commit hashes with timestamps + add PREREGISTRATION.md' sub-requirement has ZERO trace. No compute needed — everything required already exists in git history and only needs transcribing.

MOST IMPORTANT FINDING: the paper is currently UNDERSELLING its own strongest evidence. The GUE pre-registration genuinely satisfies exactly what the reviewer demanded — prediction commit 2edad5c (2026-07-18 17:00:21 -0400) strictly precedes result commit 3af3eb4 adding results/gue_census.csv (2026-07-19 02:21:24 -0400) by 9h21m, and I verified by inspecting the 2edad5c blob that the §4.1 prediction table naming the GUE promoter/TF-binding tasks as LEAKY was already present. main_resubmission.tex:213 asserts this priority with no checkable anchor whatsoever. Executing this item is mostly a transcription job that makes a true claim verifiable.

CAUTION FOR THE MAIN THREAD: do not let the new PREREGISTRATION.md drift into implying the CNN/deep registration was timestamped. It was not — deep_preregistration.md entered in 33b1825 together with the experiment code. Ledger D065 records that response_to_reviewers.md:114 previously made exactly this overclaim and had to be corrected. The two registrations must be graded differently in any new document: GUE = genuine priority, provable; CNN/deep = same-commit, weak. The existing supplementary.tex:143-147 wording is the correct standard to mirror.

Secondary: results/tier1_preregistration.md already contains a 'Commit anchoring' paragraph (lines 26-31) that makes the reviewer's argument almost word-for-word, and a verified-real repo-state hash 6b0360bd... (confirmed a real commit via git cat-file). This material is stranded in results/ and never surfaces in the bundle — the fix is largely about promoting existing text, not writing new claims.

No stale-file gap: paper/main.tex (older 20pp) contains none of this either, so main_resubmission.tex is strictly ahead and main.tex needs no action.

## C3 — PARTIAL (text-only)

**Remaining:**
- Retitle §3.7 at paper/main_resubmission.tex:191: change `\subsection{The mechanism, and its scope}\label{sec:r-mech}` to `\subsection{The mechanism (exploratory), and its scope}\label{sec:r-mech}`. Keep the `\label{sec:r-mech}` unchanged — it is cross-referenced at main_resubmission.tex:198 ('the dataset from which \S\ref{sec:r-mech} scopes the mechanism away') and at :210. Do not drop 'and its scope': that clause carries the human_nontata_promoters scoping the section depends on.
- Split paper/main_resubmission.tex:192 so the corrected-gap analysis sits behind an explicit exploratory header. Leave the raw-gap sentences ('On \textit{human_enhancers_ensembl} the near-default forest scores $1.000$ ... worse than the linear SVM ($0.782$).') as the confirmatory opening, then insert a new `\paragraph{Exploratory: the prevalence-corrected gap.}` immediately before 'Correcting the class-composition confound...' so everything from that sentence through 'a separation the raw statistic does not produce.' falls under it.
- In that same new exploratory paragraph at main_resubmission.tex:192, make the post-hoc label travel WITH the numbers instead of living only at :189. Amend the opening clause to read approximately: 'Correcting the class-composition confound---a statistic defined after P1 was committed (\S\ref{sec:r-rank}), so what follows is exploratory and is not scored as a confirmation---\emph{strengthens} the mechanism:'. Do not delete or weaken the existing :189 sentence; the two must agree.
- Add a forward registration. Preferred site: append a final sentence to the new exploratory paragraph at main_resubmission.tex:192, e.g. 'We register the corrected gap prospectively for the next leaky dataset audited: $1$-nearest-neighbour will carry the largest prevalence-corrected graded gap, and the four memorization-prone learners will average above the other five. On \textit{human_nontata_promoters} that prediction already fails ($+0.122$ against $+0.179$), so it is registered as falsifiable rather than as support.' Name a concrete next target — the two already in the paper are the break-a-clean arm on \textit{drosophila_enhancers_stark} and the chromosome-14/X stratum, both at main_resubmission.tex:256.
- Mirror the forward registration in the pre-registration paragraph at paper/main_resubmission.tex:80 by adding one clause after the P1 description, e.g. 'the prevalence-corrected form is registered forward, not counted backward (\S\ref{sec:r-mech})'. This is what closes the loop the reviewer is complaining about: the raw form was registered, the corrected form was not, and the corrected form now gets its own forward commitment.
- Fix the orphaned roster table, which the region C3 touches depends on. Add `\input{tab_roster}` inside a `\begin{table}...\end{table}` in paper/supplementary.tex (a natural home is inside \S\ref{sec:s-gap}, near line 128), give it `\label{tab:roster}`, and port the caption from paper/main.tex:242 — but STRIP the sentence 'Gap$_{\text{bal}}$ ... is the statistic we rely on' and 'Note that the corrected column orders the models as the memorization account predicts and the raw column does not', which are exactly the confirmatory framing C3 objects to. Replace with wording that marks Gap_bal exploratory and Gap_raw as the registered statistic.
- Repoint the three broken roster cross-references once the table exists: paper/supplementary.tex:100 ('The nine-model roster itself is Table~3 of the main text' → point at the new supplementary table number), paper/main_resubmission.tex:177 ('Supplementary Table~S2' → the new roster table's actual number; S2 is currently the Jaccard sensitivity table at supplementary.tex:355), and the stale comment at paper/main_resubmission.tex:7 which claims supplementary.tex already \inputs tab_roster.
- Update the occurrence counts in audit/tools/check_numbers.py:242-246 after the edits. Adding tab_roster to the bundle restores '0.461' to 3 and '0.370' to 2 (matching the current expectations), but adding a forward-registration sentence that quotes any of these values will push the counts past the expectation and re-break the gate. Also fix the two already-wrong expectations the gate reports independently of C3: '+0.179' expects 3 but finds 4, and '$0.96$' expects 2 but finds 4.
- Add a bullet to paper/response_to_reviewers.md recording the three C3 actions (retitle, exploratory header, forward registration), placed alongside the existing post-hoc disclosure at :151 and :634 so the letter does not read as if only the disclosure was done.
- Append a decision line to audit_recovered/decisions.jsonl for C3 recording the retitle + exploratory header + forward registration, with results/roster_predictions.csv (P1 False on both datasets) and results/graded_gap_corrected.csv as the evidence.
- Re-run `PAPER_TEX=main_resubmission.tex venv/bin/python -m audit.tools.check_numbers` and recompile the bundle. Baseline before any C3 edit is 109/119, RESULT: FAIL — do not read a post-edit FAIL as caused by C3 without diffing against that baseline.

**Notes:** Split verdict, and the split matters. C3 bundles two things: (a) disclose that the corrected gap is post hoc, and (b) restructure §3.7 so the framing matches the disclosure. (a) is DONE and prominent — main_resubmission.tex:189, response_to_reviewers.md:151/:634, cover_letter.md:22 all say plainly that P1 fails as registered and that the corrected reading is post hoc, not a confirmation. (b) is entirely absent: 'exploratory' is a zero-hit grep across all three .tex files, §3.7's title is unchanged, and §3.7's body still opens the corrected analysis with 'Correcting the class-composition confound \emph{strengthens} the mechanism' — confirmatory voice, no label, numbers presented as support. This is the precise pattern a prior round already caught once: results/audit_findings.csv:276 flagged that the disclaimer lives in Methods while the credit is banked in Results. The fix was applied to the scoring passage (:189) and never to the mechanism section (:191-192), so the same defect survives one subsection later.

No experiment is needed. Every number is already computed and already in the text, verified against results/graded_gap_corrected.csv to the last digit. This is a pure framing edit.

Two things the main thread should not trip over. First, do not read this as SUPERSEDED because :189 discloses the post hoc status — the reviewer's specific complaint is that §3.7 leans on the statistic, and §3.7 still does, with no marker anywhere inside it. Second, do not chase the pre-existing gate failures as if C3 caused them: `PAPER_TEX=main_resubmission.tex venv/bin/python -m audit.tools.check_numbers` is at 109/119 FAIL right now, before any edit.

Independent finding worth escalating on its own, discovered while auditing C3: the nine-model roster table is missing from the submission bundle entirely. paper/supplementary.tex contains zero \input commands, so paper/tab_roster.tex is \input by nobody; supplementary.tex:128 \ref{tab:roster} will typeset as '??'; supplementary.tex:100 misdirects to 'Table 3 of the main text' (actually tab:manip); main_resubmission.tex:177 misdirects to 'Supplementary Table~S2' (actually the Jaccard sensitivity table); and main_resubmission.tex:7 documents an \input that does not exist. The gate independently confirms this with '[FAIL] paper: table body tab_roster is \input, not hand-typed' plus the 0.461 (2 of 3) and 0.370 (1 of 2) count shortfalls, which are exactly the one occurrence each that tab_roster.tex would have contributed. The table that displays both gap forms side by side is the paper's main defense against the C3 objection, and a referee currently cannot see it.

The gate default at check_numbers.py:53 is still main.tex, the stale 20-page version. Anyone running the gate without PAPER_TEX=main_resubmission.tex is validating the wrong manuscript — the exact failure the file's own comment at :44-52 warns against.

## C4 — PARTIAL (heavy-compute)

**Remaining:**
- Wait for / re-drive PID 98413 (`audit/experiments/exp_canonical_suite.py`) to completion and confirm `results/canonical_census_suite.csv` exists with 32 rows (8 datasets x {jaccard,containment} x {0.7,0.9}) and that exp_canonical_suite.log ends with `EXP_CANONICAL_SUITE_DONE`. If the process has died, relaunch with `./venv/bin/python audit/experiments/exp_canonical_suite.py > exp_canonical_suite.log 2>&1`. Note only 2 of 16 canonical sim matrices are cached, so expect multi-hour runtime (canonical jaccard for human_enhancers_ensembl alone took ~1200s in exp_canonical.log).
- Read the `verdict_moved` column of results/canonical_census_suite.csv. If any dataset flips clean->LEAKY under canonicalization, the Table 1 verdict column in paper/main_resubmission.tex (the report card, caption at :101) must be edited and the paper's 'two leaky / one borderline / four clean' framing re-checked. If none flips, that null is itself the C4 answer and must be stated.
- Replace the hedge at paper/main_resubmission.tex:101 with the measurement. Change "a ``clean'' verdict means only *no forward-strand near-duplicate leakage detected*" to a sentence carrying the real suite-wide canonical numbers, e.g. the max over datasets of `delta`/`rel_increase` from canonical_census_suite.csv, plus the count of verdicts that moved. Keep the length-pair-cap clause.
- Add a suite-wide canonical sentence to paper/main_resubmission.tex:222, which currently reports canonical results only for the two leaky sets (drops 0.153 / 0.097). It must state that the census was rerun on all 8 datasets under both Jaccard and containment, with the observed leak_fwd->leak_canon range.
- Mirror the same suite-wide statement into paper/supplementary.tex:339, which today carries the identical two-dataset-only canonical sentence.
- Update paper/response_to_reviewers.md:588-590 so the R3.4/C4 reply cites the completed suite-wide census instead of offering the verbal downgrade of green verdicts. Point at results/canonical_census_suite.csv by name.
- Run the hashFrag both-orientation arm (A2 half of C4) — currently absent entirely. This needs the hashFrag tool + BLAST installed; hashFrag is cited (rafi2025) but never executed. Write audit/experiments/exp_hashfrag_orientation.py, emit a CSV under results/, and cite it in main_resubmission.tex. If the external dependency cannot be installed, this must be explicitly conceded in the response letter rather than left silent.
- Append a C4 decision line to audit_recovered/decisions.jsonl recording the suite-wide census result and the hashFrag disposition — no entry currently exists for this item.
- Side fix (found while checking the hashFrag citation): paper/references.bib:12-15 asserts "The current submission (main_resubmission.tex) cites rafi2026, the v2 of the same preprint." This is false — `grep -o 'rafi20[0-9][0-9]' paper/main_resubmission.tex` returns rafi2025 twice (lines 51 and 172). Either switch main_resubmission's two citations to rafi2026 or correct the bib comment.

**Notes:** Verdict is PARTIAL, and the split is clean: half the item is in flight but has produced nothing, the other half does not exist.

The single most important fact: exp_canonical_suite.py was written specifically for C4 (its docstring restates the reviewer's complaint almost word for word) and IS RUNNING RIGHT NOW as PID 98413, started 01:21AM. But it has emitted zero rows — the log is one banner line and results/canonical_census_suite.csv does not exist. The script writes the CSV incrementally after every dataset, so not a single dataset has completed. Do not mistake the script's existence or the live process for a result.

Runtime warning for the main thread: the datacache holds only 2 of the 16 canonical similarity matrices this census needs (both jaccard, both on the leaky sets), and zero canonical containment matrices. human_ensembl_regulatory has no sim cache at all. exp_canonical.log shows canonical jaccard on human_enhancers_ensembl alone took ~1200s. This job is plausibly many hours; budget for it rather than blocking on it.

What the bundle does today instead of the fix is a verbal hedge — main_resubmission.tex:101 and response_to_reviewers.md:588-590 downgrade green verdicts to "no forward-strand near-duplicate leakage detected." That is an honest disclosure and it is well written, but it is a concession of the reviewer's point, not the rerun the reviewer asked for. If the census comes back with no verdict moving, the hedge can stay and simply be backed by numbers; if any verdict moves, Table 1 and the paper's headline "two leaky / four clean" framing are directly affected.

The hashFrag both-orientation arm is genuinely NOT_DONE, not merely unreported. hashFrag exists in this repo purely as a bibliography entry. It also carries an external dependency (hashFrag + BLAST), so it is the one piece here that may not be executable locally — if so, say so in the letter rather than leaving it unaddressed, because the reviewer named it explicitly.

Also worth flagging: zero of the 66 ledger lines mention canonical, strand, orientation, or hashFrag. Whoever launched the suite run did so without recording a decision, so there is no prior context to inherit on this item.

## C5 — PARTIAL (cheap-compute)

**Remaining:**
- Add a cohesion column to results/leakage_report_card.csv and change the nontata verdict. Concretely: add header field `largest_cluster_cohesion` and set `human_nontata_promoters` -> `0.0835` (source: results/certify_nontata.json `largest_cluster_cohesion`) and `human_enhancers_ensembl` -> `0.96` (the value already quoted at main_resubmission.tex:222); change results/leakage_report_card.csv:2 verdict field from `LEAKY` to `leaky-but-correction-unsafe`. Leave line 3 (ensembl) as `LEAKY`. Note the certify JSON flags `cohesion_estimated_on_subsample = True` — disclose that in the caption.
- Fix the generator, not the .tex. In audit/pipeline/emit_tables.py:135 replace the two-way `vtex = r"\textbf{LEAKY}" if verdict == "LEAKY" else verdict` with a three-way map, e.g. `LEAKY -> \textbf{LEAKY}`, `leaky-but-correction-unsafe -> \textbf{LEAKY}$^{\ddagger}$ (correction-unsafe)`, `borderline -> borderline$^{\dagger}$`, else passthrough. Do NOT hand-edit paper/tab_reportcard.tex — audit/tools/check_numbers.py:669-674 regenerates and diffs it, so a hand edit fails the build gate. Re-run `python -m audit.pipeline.emit_tables` to regenerate paper/tab_reportcard.tex.
- Extend the Table 1 caption at paper/main_resubmission.tex:101 to define the third tier and state the cut. Add after the existing Verdict sentence: a clause defining `leaky-but-correction-unsafe` as a LEAKY dataset whose largest-cluster cohesion falls below 0.5, meaning components are single-linkage chains rather than near-duplicate sets, so whole-cluster re-splitting removes genuine signal and the novel-stratum readout on the as-shipped split should be reported instead. Give both cohesion values (0.96 ensembl, 0.083 nontata) and add the `$^{\ddagger}$` footnote naming human_nontata_promoters.
- Propagate to the abstract at paper/main_resubmission.tex:36 (the \textbf{Results:} paragraph). It currently never qualifies nontata. Add one clause, e.g. after the ranking sentence: that of the two leaky datasets only human_enhancers_ensembl supports the ranking claim, while human_nontata_promoters is certified `leaky-but-correction-unsafe` (largest-cluster cohesion 0.083, a 420/420 single-class single-locus tiling) and its demotion is accuracy-only. Keep it short — the 8pp abstract is already dense and this is an OUP 8-page limit.
- Propagate to Fig 4 (fig:census) marker style. In paper/Fig/fig_census_data.csv add a `correction_unsafe` boolean column, True only for the human_nontata_promoters row. In paper/Fig/make_fig_census.py: the fill channel at line 120 is already spent on `prereg_leaky`, so use a distinct free channel — e.g. add a grey ring/halo via a second scatter pass, or `hatch`/`edgecolor` + increased `linewidth` — for correction-unsafe points. Add a matching legend handle next to the existing `label="registered leaky in advance"` entry at line 158. Re-run the script to regenerate paper/Fig/fig_census.pdf and .png.
- Update the Fig 4 caption at paper/main_resubmission.tex:245 to explain the new marker, since the caption currently enumerates every visual channel (shape = task type, colour = organism, open = GUE prereg) and would otherwise be incomplete.
- Reconcile the two remaining two-way tallies with the new three-tier scheme: paper/main_resubmission.tex:96 ("Two datasets are leaky, one borderline, four clean") and paper/main_resubmission.tex:262 (Conclusion, "Two of the seven binary datasets are leaky, one borderline under a length-robust metric and four clean"). Either restate as one leaky / one leaky-but-correction-unsafe / one borderline / four clean, or add an explicit qualifier so the counts do not contradict the new Table 1 verdict column.
- Optional consistency check after regenerating: re-run `python -m audit.tools.check_numbers` and confirm the tab_reportcard regeneration diff at check_numbers.py:669-674 passes, and that the phrase-scoped verdict guard at check_numbers.py:761-771 (which scans for 'verdict'/'leaky'/'clean' near 'changes no verdict') does not trip on the new tier string.

**Notes:** The tool-side work is genuinely DONE and verified — certify.py:414-424 implements the third tier and results/certify_nontata.json proves it ran, returning verdict=\"leaky-but-correction-unsafe\" with largest_cluster_cohesion=0.0835. No new experiment is needed; the number already exists. What is missing is purely the propagation the item asks for, and all three named targets are untouched: Table 1 (tab_reportcard.tex:7 renders a bare \\textbf{LEAKY}), the abstract (main_resubmission.tex:34-39, no mention), and Fig 4's marker style (make_fig_census.py:120 encodes only prereg_leaky). So: evidence done, plumbing not done.

Three things the main thread should know before executing:

1. TRAP — do not hand-edit paper/tab_reportcard.tex. Its header says GENERATED, and check_numbers.py:669-674 re-runs build_reportcard() and diffs the output. A manual edit will pass visually and then fail the build gate. The edit must go into results/leakage_report_card.csv plus audit/pipeline/emit_tables.py:135.

2. FIG 4 AMBIGUITY — worth a sanity check with the author. In the CURRENT 8pp bundle Fig 4 is fig:census (main_resubmission.tex:244), confirmed by counting the only four includegraphics calls. But in the stale 20-page paper/main.tex the figure list is different (fig:controls :204, fig:ranking :219, fig:graded :258), so if the C5 item was drafted against the old numbering, \"Fig 4\" may have meant a different panel. The census figure is nonetheless the right place for a per-dataset tier marker, and fig:ranking (Fig 3, main_resubmission.tex:181) already carries the qualifier in its caption text (\"and, under accuracy only, on human_nontata_promoters\"), so census is the defensible target. Flagging so nobody assumes the numbering carried over.

3. CONSISTENCY RISK ALREADY LIVE — paper/response_to_reviewers.md:238 and :412 both promise the reviewer that the tool returns leaky-but-correction-unsafe below cohesion 0.5, and supplementary.tex:192 states it, and main_resubmission.tex:222 states it with full numbers. Table 1 is therefore currently the ONLY place in the bundle that still shows a bare LEAKY for this dataset — a reviewer following the response letter to the table will find the exact mismatch C5 describes. That makes the Table 1 fix the highest-value single edit of the seven listed.

Also note results/audit_findings.csv:350 and :494 (both `confirmed`) flag a related but SEPARATE defect in the same CSV: results/leakage_report_card.csv still carries a `ranking_inverts` column and a two-way hard-coded verdict in audit/pipeline/report_card.py:49 (`\"LEAKY\" if l7 > 0.1 else \"clean\"`). Whoever regenerates that CSV for C5 will collide with report_card.py's generator, which does not know about cohesion at all. Decide whether C5's fix edits the CSV in place or requires teaching report_card.py the third tier — otherwise the next report_card.py run silently reverts the C5 change. This is the most likely way the fix regresses.

No ledger entry exists for C5, so nothing was previously decided or deferred on it; the omission looks like an oversight rather than a deliberate scope call.

## C7 — PARTIAL (external-dependency)

**Remaining:**
- INDELS — add the generator. In audit/experiments/exp_estimator_sensitivity.py, alongside `mutate` (:83) and `shifted` (:92), add `def indel(rng, s, n_bp, kind)` producing an insertion of `n_bp` random bases at a uniform-random interior position and a deletion of `n_bp` contiguous bases, each re-padded to preserve original length (match `shifted`'s convention of appending `rand_seq` so length is held constant and the length-dependence story stays clean).
- INDELS — extend the case loop at exp_estimator_sensitivity.py:105-114 to emit indel cases at n_bp in {1,2,3,5,10} for both insertion and deletion, at all six existing lengths (70, 101, 200, 300, 500, 999). Keep n_trials=300 and the existing rng seeding so the new rows are directly comparable to the substitution rows.
- INDELS — rerun `python -m audit.experiments.exp_estimator_sensitivity`, regenerating results/estimator_sensitivity.csv. Expect ~66 existing + ~60 new rows. Confirm the run completes by checking the new `relationship` values appear in the CSV.
- INDELS — add indel rows to Table tab:sens at paper/supplementary.tex:360-370. Insert rows for '1 bp indel', '5 bp indel', '10 bp indel' (or insertion/deletion split if they diverge) in the 70/200/300/500 bp columns, taking `jaccard_detect_rate` from the new CSV exactly as the existing five rows do. Update the caption at :355-358 — the clause 'the containment index detects every one of these perturbations at every length shown' MUST be re-verified against the new `containment_detect_rate` values and narrowed if any indel breaks it.
- INDELS — add one to two sentences of S4 prose after paper/supplementary.tex:351 stating what the indel calibration shows, i.e. whether a k-mer Jaccard degrades faster under an indel than under the same number of substitutions (it should: one indel disrupts k consecutive k-mers, so a 1 bp indel is roughly as damaging as ~8 point mutations at k=8 — state the measured number, not this prediction).
- ALUY PROVENANCE — write audit/experiments/exp_repeat_alu.py that reproduces the ~7x in-repo. It must rebuild the exact leaked/novel strata used by exp_repeat.py (assert n_leaked/n_novel == 2037/4971 for nontata and 11774/18770 for ensembl as a guard), score AluY content, and emit results/repeat_alu_enrichment.csv with per-dataset leaked_rate, novel_rate, enrichment ratio and a Fisher exact p. Without this the ~7x printed at main_resubmission.tex:222 and supplementary.tex:339 is an uncited number with no reproducible source in the repository.
- ALUY PROVENANCE — heed audit_recovered/HIGH_SEVERITY.md:474-476: do NOT run RepeatMasker, and do NOT report a single probe percentage (absolute rates swing 1.0-3.9% across probe sets). Use the UCSC hg38 RepeatMasker track intersected against the interval coordinates (results/assembly_verification.csv establishes GRCh38 at 101/101, so the track is well-founded). If the track is unavailable, run at minimum the three disjoint AluY consensus probe sets described at HIGH_SEVERITY.md:461-464 and report the RANGE (7.8x, 5.6x, 7.3x) rather than a bare '~7x'.
- REPEATMASKER JACCARD STRATIFICATION (the explicitly-requested deliverable, currently absent entirely) — compute the 8-mer Jaccard distribution separately for RepeatMasker-annotated pairs vs non-repeat pairs, and add it to S4. The pair-level similarity matrices already exist at datacache/sim_human_nontata_promoters_k8_jaccard.npy and datacache/sim_human_enhancers_ensembl_k8_jaccard.npy, so this is a join against a repeat-overlap flag per sequence, not a fresh all-pairs computation. Report median/upper-tail Jaccard and the fraction crossing the 0.7 threshold in each stratum, into results/repeat_jaccard_strata.csv, then cite those numbers in S4.
- BOTH DATASETS — the manuscript currently reports only the nontata enrichment. Add the ensembl direction (AluY-DEPLETED in the leaked stratum, 0.0024 vs 0.0106 per HIGH_SEVERITY.md:466) to supplementary.tex:339. Reporting only the dataset that supports the repeat story while omitting the one that runs opposite is exactly the selective-quotation failure mode gated in commit cbeb235.
- CODE HYGIENE — fix audit/experiments/exp_repeat.py docstring line 5 and the trailing READING print at :64-66, which still assert 'discrete locus duplication, not TE-family expansion'. This inference was invalidated (HIGH_SEVERITY.md S13) and the fix was applied to the prose but not the shipped code. Narrow both to 'excludes a microsatellite/low-complexity artifact; silent on interspersed repeats'.
- CODE HYGIENE — update results/NEW_FINDINGS.md:86, which still records the superseded opposite conclusion ('NOT a TE-family / microsatellite artifact'), so the results notes do not contradict main_resubmission.tex:222.
- GATE — after the numbers land, add the new indel detect-rates and the AluY enrichment figure to audit/tools/check_numbers.py so they are machine-checked against their CSVs like the other manuscript numbers, rather than hand-typed (this is the pattern established in commit 9c04d58).

**Notes:** HEADLINE: C7 is PARTIAL, but the most serious problem is not the missing work — it is that the one part which LOOKS done is an unsourced number. The '~7x AluY-enriched' claim is asserted twice in the canonical bundle (main_resubmission.tex:222, supplementary.tex:339) while `grep -rIl -i 'aluy|repeatmasker'` over the whole repo hits no script, no CSV, and no log. Its actual origin is an auditor's probe recorded only as prose in audit_recovered/HIGH_SEVERITY.md:461-465. The manuscript is currently citing a reviewer's own uncommitted calculation as its own finding, with '(Supplementary \\S S3)' pointing at a section that does not derive it. If a referee asks for the AluY provenance today, there is nothing to hand over. That should be treated as the priority, above the indel table.

CONTRADICTION FOUND: the repo's recorded conclusion and the manuscript's current claim point in opposite directions and both are still live. results/NEW_FINDINGS.md:86 and audit/experiments/exp_repeat.py:5,64-66 both still assert the leakage is 'NOT a TE-family artifact', while main_resubmission.tex:222 now asserts a 7x TE enrichment. The prose was updated; the code and the results notes were not. Anyone reproducing from the shipped script gets the old, invalidated reading printed to their terminal.

SELECTIVE-REPORTING RISK: the enrichment holds on nontata (7.8x/5.6x/7.3x) but INVERTS on ensembl (0.0024 vs 0.0106, depleted — HIGH_SEVERITY.md:466). The bundle reports only nontata. Since commit cbeb235 explicitly gated the selective-quotation class, shipping the favorable dataset alone is inconsistent with a rule this project already adopted.

SCOPE HONESTY on effort: I marked external-dependency because the item as written says 'RepeatMasker-annotated', which needs the UCSC hg38 rmsk track — nothing repeat-annotated exists in datacache/ (checked recursively). However 2 of the 3 sub-parts are genuinely cheap: the indel calibration is a ~20-line addition to an existing script whose sibling (exp_repeat.py) runs in 2s, and the AluY reproduction runs against strata that already exist. The indel arm and the AluY provenance script can both land without any download; only the repeat-vs-non-repeat Jaccard stratification is blocked on the track, and even that reuses the pre-computed datacache/sim_*_k8_jaccard.npy matrices rather than needing a fresh all-pairs run.

NO LEDGER ENTRY: decisions.jsonl is 66 lines, all 66 parse as valid JSON (no malformed lines encountered), and none references indels, AluY, RepeatMasker, or S4 calibration. C7 was never triaged.

NO REVERSE GAP: checked paper/main.tex (the stale 20-page version) — its line 346 still carries the fully un-corrected 'rather than a transposable-element or microsatellite artifact' claim, so main_resubmission.tex is ahead of it. Nothing is fixed in the stale file that is missing from the canonical one. paper/response_to_reviewers.md never mentions AluY, indels, or repeats in this connection.

FILE-STATE NOTE: paper/main_resubmission.tex and paper/supplementary.tex are UNTRACKED (`git ls-files paper/` returns neither). `git log -S'AluY' -- paper/` returns nothing, confirming the AluY sentence has never been in a commit — it exists only in the working tree.

## C8 — PARTIAL (cheap-compute)

**Remaining:**
- Write audit/experiments/exp_published_baseline.py that imports the suite's own published model: `from genomic_benchmarks.models.torch import CNN` (installed at venv/lib/python3.13/site-packages/genomic_benchmarks/models/torch.py). Instantiate CNN(number_of_classes=2, vocab_size, embedding_dim, input_len, device) — note it takes INTEGER-TOKENIZED input via nn.Embedding, not the one-hot encoding exp_deep.py:60 builds, so a new tokenizer path is required (genomic_benchmarks.dataset_getters.utils has LetterTokenizer/build_vocab helpers).
- Run that published CNN on human_enhancers_ensembl on BOTH arms (as-shipped split and the corrected near-duplicate-aware split), reusing the exact partition objects exp_deep.py already builds so the comparison is arm-identical. Use >=5 training seeds to match the tolerance the paper already reports for its own CNN (exp_deep_cnn_seeds.csv sets that precedent).
- Write results/exp_published_cnn.csv with columns matching results/exp_deep_cnn_seeds.csv (dataset, seed, acc_orig, acc_corr, drop, drop_ci_cluster_lo/hi, drop_excl0_cluster) so audit/tools/check_numbers.py can gate it the same way.
- Compute the published CNN's rank on BOTH arms against the existing nine-model roster in results/roster_rankings.csv (as-shipped comparators: MLP 0.8736, kNN-1 0.8729, ExtraTrees 0.8721, RF 0.8596, LinearSVC 0.7980; corrected comparators: LinearSVC 0.7975, LR 0.7806, MLP 0.7684). Report rank_orig -> rank_corr as a leaderboard delta out of ten. Use the SAME cluster-bootstrap resampling unit as the rest of the roster so the interval is comparable.
- Rewrite paper/main_resubmission.tex:232 so it no longer answers a question about the published baseline with a from-scratch proxy. Concretely: keep the first sentence, then add the published baseline's measured as-shipped and corrected accuracy and its rank delta, and state whether it moves in the same direction as the from-scratch network. This is the sentence that converts 'post hoc tenth entrant' into 're-evaluation of the suite's own baseline'.
- Add the published CNN as a row in the roster table (paper/Fig/tab_roster.tex, generated — regenerate from source CSV per the note at paper/main_resubmission.tex:7, do NOT hand-inline it; ledger D059(b) already flagged hand-inlining of tab_roster as a gate failure).
- Add a short subsection to paper/supplementary.tex next to \subsection{From-scratch 1D CNN...} at line 133 documenting the published baseline's architecture (embedding -> 3x[Conv1d k=8, 16/8/4 channels, BN, MaxPool] -> Linear 512 -> Linear 1), its Adam/epoch training protocol as shipped in the package, the package version pinned, and explicitly why TWO CNNs are now reported (from-scratch = no pretraining confound probe; published = the suite's own leaderboard entry).
- Update paper/response_to_reviewers.md:51 — change '**A nine-model roster, and a from-scratch CNN as a tenth entrant**' to state that the suite's own published baseline was re-evaluated on the corrected split, with the delta. Also update :84, :110, :352 and the section-map row at :663 which all currently describe the deep arm as from-scratch only.
- Decide the fate of results/sanity_check.md (mtime May 23, pre-revision). It holds the only published-baseline numbers in the repo (84.6%/86.5% on human_nontata_promoters, quoted from Gresova Table 2, not run by us) and its line 42-46 reasoning — 'the published CNN numbers were themselves measured on this same leaky split, so they are inflated by the same homology leakage' — is exactly the C8 argument. Either promote that argument into main_resubmission.tex:232 backed by the new run, or mark the file stale so a reader does not mistake quoted literature values for measured ones.
- Append a decision entry to audit_recovered/decisions.jsonl recording C8: that the published Gresova baseline was re-run on corrected splits, the numbers obtained, and whether the rank delta reproduces the from-scratch CNN's 5->1. No entry currently exists for this item.

**Notes:** CORE FINDING: the manuscript answers 'why a post-hoc tenth entrant?' with prose (main_resubmission.tex:232 correctly observes the suite's own baseline is a CNN), and it does show a real, well-supported CNN leaderboard delta (rank 5 of ten as shipped -> rank 1 corrected). But that delta belongs to the authors' OWN from-scratch Basset/DeepSEA-lineage ResCNN, not to the published Gresova model. The item asks specifically for the published baseline, and the hard proof it never ran is that `genomic_benchmarks.models` is never imported anywhere under audit/ (zero grep hits), while the model class itself sits installed and ready at venv/lib/python3.13/site-packages/genomic_benchmarks/models/torch.py:11.

WHY THIS IS PARTIAL AND NOT NOT_DONE: the experimental machinery, the corrected splits, the cluster-bootstrap intervals, and the nine-model roster all exist and ran. What is missing is one additional model in an existing harness. The scientific claim C8 wants ('the suite's own baseline is demoted by its own split') is currently made by proxy rather than directly.

WHY THIS IS NOT SUPERSEDED: the bundle did not dissolve the item — it sharpened it. main_resubmission.tex:232 raises the published baseline by name and then substitutes a different network, and response_to_reviewers.md:51 uses the literal phrase 'a from-scratch CNN as a tenth entrant'. A reviewer reading both will land on exactly the objection C8 states.

TWO IMPLEMENTATION HAZARDS THE MAIN THREAD MUST NOT MISS:
(1) INPUT REPRESENTATION MISMATCH. exp_deep.py:60 builds one-hot 4-channel input. The published CNN starts with nn.Embedding(vocab_size, embedding_dim) and expects integer token IDs (torch.long), per its count_flatten_size at torch.py:52-57 which feeds torch.zeros([1,input_len], dtype=torch.long). You cannot feed the existing one-hot tensors to it. A tokenizer path must be written or the comparison will be silently wrong.
(2) FIXED input_len. count_flatten_size bakes the flatten dimension in at construction time, so the published CNN requires a fixed sequence length. human_enhancers_ensembl has VARIABLE lengths — padding/truncation policy must be chosen and DISCLOSED, because that choice is a degree of freedom that affects the reported accuracy.

STATISTICAL CAUTION GROUNDED IN THIS REPO'S OWN HISTORY: ledger D057 records a real blocker where a narrower interval was silently substituted for a wider one on the CNN arm — 'an objection answered by a narrower interval rather than better evidence'. And D064 records the main thread inventing a ratio ('twentyfold') in a prompt that an agent then transcribed into the manuscript. When reporting the published CNN's delta, quote the interval the CSV actually carries, use the same cluster-bootstrap unit as the rest of the roster, and do not compute any ratio that is not printed in a file.

HONEST-REPORTING RISK: the published CNN may well show LITTLE inflation, since main_resubmission.tex:232 already concedes the from-scratch CNN's inflation is 'between six- and thirtyfold smaller than the forest's'. If the published baseline does not invert, that is a null that must be reported as such — it would narrow Claim II's scope, and the paper's own framing (main_resubmission.tex:230, 'memorization propensity, not capacity') already predicts a regularized CNN resists the effect. Do not run this expecting a confirmation; the repo has been Reject-Revised for overclaiming (per ledger D065) and a manufactured positive here would be the worst place to add one.

## D7 — PARTIAL (text-only)

**Remaining:**
- Edit paper/response_to_reviewers.md, replacing lines 458-467 so the letter matches main_resubmission.tex:65 — lead with the unsampled check, then the sample, then the denominator sentence. Suggested replacement: 'The assembly underlying that argument is likewise now **measured, not asserted** (§2.2), and the unsampled check is the stronger one. All 190,973 shipped intervals of the two leaky datasets name a primary chromosome and none an alternate contig, and — without sampling, so with no denominator to choose — **915 of them (0.48%) end past a GRCh37 chromosome boundary while none ends past a GRCh38 one.** A sampled sequence-level check agrees. For 49 test intervals stratified over both datasets, classes and strands, the reference was fetched at the shipped coordinates from both GRCh38 and GRCh37 via the UCSC REST API, reading intervals as 0-based half-open and reverse-complementing minus-strand records: **GRCh38 reproduces the shipped sequence in 49 of 49 cases and a 1-based reading in none of 49.** The GRCh37 denominator is 48, not 49, for the same reason the boundary count gives: one sampled chrX interval runs past the GRCh37 chromosome end, so the API returns an error rather than a sequence and that row is unevaluable on GRCh37 by construction. On the 48 evaluable rows GRCh37 reproduces the shipped sequence once.'
- Grep the whole repo one more time after the edit for any surviving '101 of 101', '101/101', '1 of 48', 'n=101' outside audit_recovered/ (which is append-only history and should NOT be rewritten) and confirm zero hits in paper/.
- Regenerate the rendered letters, which are stale anyway: run bash paper/build_letters.sh (or the project's equivalent) to refresh paper/response_to_reviewers.pdf and paper/response_to_reviewers.docx from the corrected .md.
- Optional but cheap and closes the reproducibility hole: no script in the repo computes the 915 / 190,973 boundary census — it is asserted in prose and only narrated in audit_recovered/INDEPENDENT_TRACKS.md:254-257. Add a short experiment (e.g. audit/experiments/exp_assembly_boundary.py) that reads datacache/intervals/{human_enhancers_ensembl,human_nontata_promoters}/*.csv.gz against api.genome.ucsc.edu/list/chromosomes and writes results/assembly_boundary_census.csv with columns dataset,n_intervals,n_nonprimary,n_past_grch37,n_past_grch38. I have already verified it emits exactly 154842/36131/0/915/0, so this is transcription, not discovery. This matters because the item promotes the boundary check to PRIMARY evidence and primary evidence should have a regenerable artifact.
- Optional: append a decision to audit_recovered/decisions.jsonl recording that the assembly sample was re-run at n=49 (13 per class per dataset, seed 20260719) superseding D010's 101/48/49, and that the boundary census is now the primary evidence.

**Notes:** Main text is genuinely and completely fixed; the only defect is the response letter. Do not let the main_resubmission.tex quality mislead you into marking this DONE.

Three things worth flagging to the main thread:

1. The inconsistency was fixed by RE-RUNNING the experiment smaller, not by explaining the old gap. The old CSV had 101 rows with only 48 GRCh37-evaluable (53 unexplained dropouts); the current CSV has 49 rows with 48 evaluable (1 dropout, explained). This is a legitimate and cleaner fix, but note the sampled n dropped 101 -> 49, which weakens the sampled arm's Clopper-Pearson bound (0.05^(1/101)=0.971 becomes 0.05^(1/49)=0.941). That loss is exactly why promoting the unsampled 915/190,973 census to primary was the right call, and main_resubmission.tex:65 does promote it. If a reviewer asks why n shrank, the honest answer is that the 101-row run had 53 rows the GRCh37 arm could not evaluate and the re-run traded sample size for a clean, fully-evaluated design. Consider whether to say so; currently nothing in the bundle explains the change from the previously-submitted 101.

2. The response letter is not merely stale, it is now falsifiable against the repo's own data. It claims 101 of 101 while results/assembly_verification.csv holds 49 rows. A reviewer with the artifacts can catch this. Treat the letter edit as required, not cosmetic.

3. I independently recomputed the promoted primary evidence end-to-end from datacache/intervals against live UCSC chromosome lists and got 190,973 / 0 non-primary / 915 (0.4791%) / 0 — an exact match on all four figures, including the 0.48% rounding. The chrX unevaluable explanation also checks out arithmetically (156,002,884 > hg19 chrX 155,270,560, <= hg38 chrX 156,040,895). The science here is sound; only the letter's prose lags.

Also note paper/response_to_reviewers.pdf and .docx (23:39) predate the .md (00:18), so they are stale for reasons beyond D7 — whatever other items touched the letter also need the rebuild. Batch the rebuild once after all letter edits land rather than per item.

## E1 — PARTIAL (cheap-compute)

**Remaining:**
- Decide the route. Recommended: Welch-Satterthwaite rather than flat t_4, because the effective df differs sharply across the three intervals (119, 35, 8.1) and flat t_4 is unjustifiably conservative on the two re-split intervals where the bootstrap component dominates. If a flat t_4 is chosen instead, note it kills the CNN-vs-LinearSVC claim (see next item).
- Patch audit/experiments/exp_clusterboot_full.py:61 — replace `cb_lo, cb_hi = delta - 1.96 * comb_sd, delta + 1.96 * comb_sd` with a t-multiplier. Add `from scipy import stats` (NOTE: scipy is NOT installed in the default python3 on this machine — `python3 -c 'from scipy import stats'` fails with ModuleNotFoundError; either pip install into the project env or hardcode the critical value with a comment giving its provenance).
- Patch audit/experiments/exp_deep_paired.py:150 — same substitution for `clo, chi = point - 1.96 * comb_sd, point + 1.96 * comb_sd`.
- Also emit the df and the critical value into both CSVs so the number is auditable: add `df=` and `tcrit=` columns to results/cluster_bootstrap_full.csv (written at exp_clusterboot_full.py:65-70) and results/exp_deep_cnn_paired.csv (written at exp_deep_paired.py:155-160, which already stores seed_sd/boot_sd/combined_sd).
- Rewrite paper/main_resubmission.tex:91. Replace `\delta\pm z\sqrt{s^{2}_{\mathrm{boot}}+s^{2}_{\mathrm{seed}}}` with the t form, and add an explicit approximation disclosure naming the df problem — that s_seed carries only 4 df from five seeds, that the normal quantile therefore understates the interval, and which multiplier was used. This single sentence is the core of the item.
- Update the three quoted intervals wherever they appear once recomputed: main_resubmission.tex:172 ([15.4,17.4] and [8.8,12.9]), main_resubmission.tex:185 ([0.0019,0.0204]), main_resubmission.tex:262 (discussion repeats [15.4,17.4] and [8.8,12.9]), cover_letter.md:16, response_to_reviewers.md:141 and :542.
- Fix the s_seed mismatch at main_resubmission.tex:172: the text prints \pm0.0019 and \pm0.0055 (from results/step2_seed_variance.csv) but the intervals were built with seed_sd 0.0021 and 0.0062 (results/cluster_bootstrap_full.csv). Either quote the values actually used in the interval, or state plainly that the printed \pm is the seed spread of corrected accuracy while the interval folds a separately-computed s_seed.
- Add a short paragraph to paper/supplementary.tex in the statistics section (near the design-effect paragraph at :195-201) documenting the combined-source construction: the two variance components, that s_seed has 4 df, the multiplier used, and the per-interval df. The supplement is currently completely silent on this.
- Update the guards in audit/tools/check_numbers.py that pin the old numbers, or the checker will fail: :361 and :365-366 (nontata combined-source CI), :617-623 (CNN combined_ci_lo/hi in paper, response, cover), and the tamper-detection fixture at :853-854 which hardcodes the string "$[0.0019,0.0204]$".
- Write a decision entry to audit_recovered/decisions.jsonl recording the route chosen and, critically, the honest disclosure that the CNN-vs-LinearSVC combined interval is the one sensitive to the choice — flat t_4 makes it [-0.0021,+0.0243], which includes zero. Do NOT let this be settled silently in the flattering direction; D057 in the same ledger records exactly that failure mode being caught before.

**Notes:** The core of E1 is NOT done: main_resubmission.tex:91 still literally writes the formula with z, and both scripts hardcode 1.96. Neither alternative the reviewer offered is fully taken — no t_4, and no 'approximate' label. What IS done is the second half of option (b): the seed SD is reported separately, explicitly and well for the CNN interval (main_resubmission.tex:185 gives 0.0040 seed and 0.0026 bootstrap), and as a seed spread for the two re-split deltas (main_resubmission.tex:172). That is why this is PARTIAL rather than NOT_DONE.

The main thread must know one thing before choosing a route. Only one of the three intervals is sensitive, and it is the load-bearing one. Recomputed from the stored combined_sd values: the two re-split intervals survive t_4 comfortably ([15.0,17.8] and [7.9,13.8], both still excluding zero), but the CNN-vs-LinearSVC interval goes from [0.0019,0.0204] to [-0.0021,+0.0243] and INCLUDES ZERO. That interval backs the sentence at main_resubmission.tex:185 claiming the CNN beats LinearSVC, and it is quoted in cover_letter.md:16 and response_to_reviewers.md:141.

The reason is structural and worth stating in the paper rather than hiding: the CNN interval is the only one where seed variance dominates (seed_sd 0.0040 vs boot_sd 0.0026), so its effective df really is near 4. The other two are bootstrap-dominated (Welch-Satterthwaite df 119 and 35). This makes flat t_4 the wrong instrument — it is far too conservative on two intervals and correctly harsh on one. Welch-Satterthwaite is the defensible answer and it happens to preserve the CNN claim, but only barely ([+0.0001,+0.0221] at df=8.1). That margin is thin enough that it must be reported as thin, not as a clean exclusion.

Two caveats on execution. scipy is not importable from the default python3 here, so the t-quantile needs either an env fix or a documented hardcoded constant. And check_numbers.py has a tamper-detection fixture at :853-854 that hardcodes the literal string "$[0.0019,0.0204]$" as the correct value — changing the interval without updating that line will trip the checker in a way that looks like tampering.

Separate minor defect found while auditing: main_resubmission.tex:172 prints seed SDs of 0.0019 and 0.0055 (from results/step2_seed_variance.csv), but the combined intervals were actually computed with 0.0021 and 0.0062 (results/cluster_bootstrap_full.csv). The reader cannot reproduce the quoted interval from the quoted SD. Since E1 is specifically about reporting the seed SD, this should be fixed in the same pass.

Finally, supplementary.tex contains nothing at all on this — zero hits for combined-source, Wald, or the variance decomposition. Its four "approximation" mentions are about Kish's design effect and the two-stratum equation, unrelated. Whatever disclosure gets written should have a home in the supplement, not only a compressed clause in the main text.

## E3 — PARTIAL (cheap-compute)

**Remaining:**
- Add the explicit membership rule at paper/main_resubmission.tex:91, replacing "over a declared family of $15$ deltas" with the rule that actually matches results/cluster_bootstrap_full.csv. Correct wording: "over a pre-declared family of the $15$ corrected-split accuracy deltas formed by the two pre-registered $k{=}6$ models (random forest, logistic regression) on each of the eight datasets of Table~\ref{tab:reportcard}, less logistic regression on the three-class \textit{human\_ensembl\_regulatory}". Do NOT use the reviewer's suggested phrase "best-model deltas" — the family is two fixed models per dataset, not the per-dataset best model, so that wording would be factually wrong.
- Resolve the 15-vs-16 boundary, which is the specific post-hoc appearance the reviewer flagged. Either (a) justify the omission of human_ensembl_regulatory|LR_k6 in one clause at main_resubmission.tex:91, or (b) preferred — add it and make the family 16. Option (b) requires running the cluster bootstrap for that one cell: results/all_results_long.csv already has its corrected-split accuracies (homology@0.5/0.7/0.9, seeds 0-2, e.g. 0.620275 at @0.5 seed 0), so add the row to results/cluster_bootstrap_full.csv and re-run `python -m audit.experiments.exp_bh_correction`. Its delta is near-null, so it will not flip the three survivors.
- Write a new script audit/experiments/exp_bh_pooled.py that concatenates results/bh_correction_frozen.csv (15 leaky-vs-clean deltas) with results/crosssuite_ranking.csv (16 cross-suite drops, columns `drop` and `drop_ci_cluster`) and runs the existing bh() from exp_bh_correction.py over the pooled 31, emitting results/bh_pooled_sensitivity.csv with columns family/label/delta/p_approx/bh_separate/bh_pooled. I already verified this computation runs in seconds and gives: pooled 31 -> 5 survivors (the 3 family-A survivors plus enhancers|RF p=2.20e-06 and enhancers|LinearSVC p=7.88e-03).
- Report the pooled sensitivity result in the manuscript. Add to main_resubmission.tex:218 (Robustness) a sentence with the real number: under a single pooled family of all $31$ deltas the three family-A survivors are unchanged, and one additional cross-suite flag, \textit{enhancers}$|$\textit{LinearSVC} ($p=0.0079$), crosses at $q=0.05$ — i.e. pooling is anti-conservative here and strengthens rather than weakens the leakage claim.
- Fix the now-unsupported clause at main_resubmission.tex:210. "Under Benjamini--Hochberg across this cross-suite grid only \textit{enhancers}$|$RF survives, so these flags are nominal" must be qualified, because \textit{enhancers}$|$LinearSVC survives under pooling. Suggested: "...only \textit{enhancers}$|$RF survives; \textit{enhancers}$|$\textit{LinearSVC} additionally survives when the two families are pooled (\S\ref{sec:r-robust}), so the remaining flags are nominal." Note this change is favourable to the paper's argument — it supports the existing "two non-memorizers exclude zero here" sentence on the same line.
- Disclose the approximation at main_resubmission.tex:91. exp_bh_correction.py:18-35 states the BH input p-values are normal approximations recovered from percentile CIs (SE=(hi-lo)/2/1.96) because the bootstrap draws were not retained. Add a half-clause: "...with two-sided $p$ recovered from the percentile intervals under a normal reference, adequate here because every delta in the family sits at $|z|>30$ or $|z|<2$." Reviewer E3 is a statistics item; an undisclosed p-value approximation feeding a declared FDR procedure is the kind of thing that draws a follow-up.
- Decide and state what happens to the third family, which currently has no correction anywhere in the bundle. results/inversion_law_bootstrap.csv already carries bh_sig_delta/bh_sig_m_inv/verdict_bh from exp_inversion_law.py:447-450, but paper/supplementary.tex presents the inversion law (lines 256-259, 296-297, 323) with no multiplicity text at all — the prose describing that correction survives only in the orphaned, un-\input'd paper/tier1_sections.tex:39. Port one sentence of that BH text ("we compute two-sided bootstrap $p$-values for every pair and apply Benjamini--Hochberg at $q=0.05$ within each estimand family; the pair survives on both, $p=4\times10^{-4}$ and $p\le10^{-4}$") into supplementary.tex, and state in the same place why this family is corrected separately from the 31 (different estimand: pairwise margins, not accuracy deltas). Leaving three families corrected under three different conventions with only one of them explained is exactly the pattern E3 objects to.
- Optional but recommended for consistency: either run `PYTHONPATH=. venv/bin/python -m audit.experiments.exp_nt_enhancers_decontam_stability` to produce the missing results/nt_enhancers_decontam_stability.csv, or delete the script. Its own docstring (lines 14-18) argues that reporting those six model-pair intervals without BH would make the paper "internally inconsistent", so an unrun script asserting a fourth family is a loose end an E3-motivated reviewer may pull.

**Notes:** SPLIT VERDICT. Half of E3 is genuinely done, half has zero trace.

Part 1 (state the membership rule): PARTIAL. main_resubmission.tex:91 and :218 both call the family "declared" and give its exact size (15, up from the vaguer "roughly a dozen" in the superseded main.tex:339), and the computation genuinely ran with real numbers in results/bh_correction_frozen.csv. But the declaration itself does not exist anywhere in the bundle — supplementary.tex, tab_reportcard.tex and tab_roster.tex all return 0 hits for hochberg/benjamini/FDR. Saying "declared" twice without ever declaring is arguably worse than saying nothing, and a reviewer who already suspects post-hoc boundaries will read it as evasion.

Part 2 (pooled sensitivity analysis): NOT_DONE. No code, no CSV, no text.

CONTRADICTION THE MAIN THREAD MUST HANDLE. I ran the pooled analysis myself and it is not verdict-neutral. Pooling family A (15) with the cross-suite grid (16) into one 31-member family flips enhancers|LinearSVC (delta 0.0221, p=0.0079) from non-significant to significant, because BH gains power as small p-values enter the family. That directly undercuts the trailing clause "so these flags are nominal" at main_resubmission.tex:210. Whoever writes the fix must edit :210, not just append a sentence at :218.

The direction is favourable, which is worth knowing before anyone gets defensive about it: LinearSVC is a non-memorizer, so its surviving correction reinforces the existing sentence on the same line ("two non-memorizers exclude zero here ... leaving the retraining penalty of §3.6 the better reading"). Reporting the pooled analysis honestly strengthens the paper's own argument. There is no incentive to bury this one.

THE REVIEWER'S SUGGESTED WORDING IS WRONG AND MUST NOT BE PASTED IN. E3 proposes 'all primary per-dataset best-model deltas in Table 1 and Section 3.5'. The actual family (verified row-by-row in results/cluster_bootstrap_full.csv) is RF_k6 and LR_k6 — two fixed pre-registered models per dataset — not the per-dataset best model. Adopting the reviewer's phrase verbatim would state a membership rule that does not describe the 15 rows actually corrected, converting a vagueness problem into a false statement. Flagging this explicitly because the instinct to mirror a reviewer's language is strong and here it backfires.

THE BOUNDARY IS GENUINELY POST-HOC, NOT JUST POST-HOC-LOOKING. The family is 15 rather than 16 solely because human_ensembl_regulatory|LR_k6 is absent from cluster_bootstrap_full.csv. That delta is computable — results/all_results_long.csv holds its corrected-split accuracies at all three homology thresholds across three seeds. Nothing in code or text explains the exclusion. The reviewer's suspicion is correct on the merits, and the cleanest response is to add the 16th delta rather than to justify its absence.

SCOPE IS LARGER THAN E3 STATES. The reviewer names two families; the repo has four. (A) the 15 frozen deltas, corrected, membership undeclared. (B) the 16 cross-suite drops, corrected separately at :210. (C) 12 pairs x 2 estimands in the inversion law — this one RAN (results/inversion_law_bootstrap.csv carries bh_sig_delta/bh_sig_m_inv/verdict_bh) but is reported NOWHERE in the current bundle; the only prose describing it sits in paper/tier1_sections.tex:39, which is orphaned and \input by nothing. (D) six model pairs in exp_nt_enhancers_decontam_stability.py, whose output CSV was never produced. Answering E3 with a pooled analysis over only A+B leaves a reviewer free to ask the identical question about C, and C is already computed — the marginal cost of covering it is one ported sentence.

Also unflagged by the reviewer: main_resubmission.tex never discloses that the BH input p-values are normal approximations recovered from percentile CIs, though exp_bh_correction.py:18-35 is candid about it and calls exact bootstrap p-values the correct long-term fix. On a statistics item this is worth a half-clause.

No ledger entry exists for E3 in audit_recovered/decisions.jsonl (only D035 matched on keywords and is unrelated), so nothing here was previously considered and consciously deferred. This item was simply never worked.

Verification caveat on my own numbers: my pooled computation reuses the same normal approximation exp_bh_correction.py uses, so it inherits that approximation's limits. The flipping row (p=0.0079 against a BH threshold of 0.00625 in the 16-member family) sits closer to its decision boundary than any row in family A, so unlike the original analysis this conclusion is NOT immune to the approximation. If the pooled result is going into the manuscript, regenerate exact bootstrap p-values with draw retention for the cross-suite grid rather than shipping my normal-reference figure. I did not run that; my number should be treated as a strong indication, not a final value.

## E4 — PARTIAL (cheap-compute)

**Remaining:**
- Add a CR2 (bias-reduced) small-cluster CRVE to audit/experiments/exp_clusterboot_full.py alongside the existing crve_mean_ci at :16-23. For the mean estimand X=1_n the hat matrix is H=(1/n)J_n, so the per-cluster adjustment has a closed form: A_g = I_{n_g} + [(1 - n_g/n)^{-1/2} - 1] * J_{n_g}/n_g. Compute meat = sum_g (1' A_g r_g)^2, se_CR2 = sqrt(meat)/n. Also emit the CR1 factor (G/(G-1))*((N-1)/(N-1)) for reference.
- Replace the z=1.96 multiplier at exp_clusterboot_full.py:23 with a Bell-McCaffrey Satterthwaite df (or, if you prefer conservative-and-simple, t_{G-1}), and record the df in the CSV so a referee can see it. State which you chose in the supplement.
- Compute the CRVE on BOTH arms, not just the as-shipped one. exp_clusterboot_full.py:63 currently passes (c_o, ocl); add a second call passing (cc, ccl) -- the corrected-arm correctness and corrected-arm clusters already computed at :46-47. This is the arm the reviewer named and the arm decision D011 says the whole design-effect analysis mischaracterises.
- Widen the CRVE from the accuracy mean to the DELTA, so it is actually a cross-check on the intervals the paper reports. Stack the two arms' correctness with an arm indicator, cluster on the union of ocl and ccl (relabelled disjointly), and take the CR2 SE of the arm coefficient. Without this, main_resubmission.tex:91's word "cross-check" is not supported by anything computed.
- Add columns to results/cluster_bootstrap_full.csv: `crve_cr0_ci`, `crve_cr2_ci`, `crve_cr2_df`, `n_clusters_corrected`, and per-arm variants (`orig_*` / `corr_*`). Rerun exp_clusterboot_full.py end-to-end and confirm all 16 rows regenerate (5 clean datasets + 3-class must survive).
- Also write the corrected-arm design-effect quantities to a committed CSV -- they currently exist only in prose and in the working note audit_recovered/INDEPENDENT_TRACKS.md:407-415. Emit corrected-arm m*, mean block size, ICC, Kish deff, largest component, %-in-multi-member, and measured cluster/sample variance ratio for both datasets and both models, so main_resubmission.tex:218's 4.45 / 2.58 / 2.80 / 2.58 / 3.96x / 2.12x / 43.4% / 47.7% become reproducible from results/.
- Insert the CR2 numbers into paper/main_resubmission.tex:218, replacing the unquantified clause at :91. Suggested: after the measured-inflation sentence, add one clause giving the corrected-arm CR2 interval for the forest delta on each dataset and stating whether it excludes zero. Do NOT leave 'a Liang-Zeger cluster-robust standard error agrees' as the only CRVE content -- a referee who asked for CR2 will grep for a number.
- Add a sentence to paper/supplementary.tex after the Design effect paragraph (:196-202) specifying the CRVE variant used: 'Cluster-robust standard errors are Liang-Zeger with the CR2 bias reduction of Bell and McCaffrey and a Satterthwaite degrees-of-freedom correction; on these data G is 5,833 and 23,360 on the corrected arms, so CR2 and CR0 agree to <N> decimal places.' Fill <N> from the actual run.
- Add three BibTeX entries to paper/references.bib, which currently cites none of them: Liang & Zeger 1986 (Biometrika 73:13-22), Bell & McCaffrey 2002 (Survey Methodology 28:169-181) for CR2, and Kish 1965 'Survey Sampling' for m* -- the last is named twice in the bundle (main_resubmission.tex:218, supplementary.tex:196) with no reference.
- Mirror the CR2 result into paper/response_to_reviewers.md:544, which currently says only 'an analytic Liang-Zeger cluster-robust standard error agrees'. Replace with the CR2 interval and an explicit note that the reviewer's small-cluster concern was checked and does not bite because G is in the thousands on both corrected arms -- this converts the item from a concession into a discharged point.
- Add a check_numbers guard: audit/tools/check_numbers.py has zero CRVE coverage. Add a check pinning the CR2 interval endpoints quoted in main_resubmission.tex and response_to_reviewers.md to the new CSV columns, in the check_shared_numbers style at :515-544 (both against the CSV and against each other).
- Separately resolve the ICC/deff inconsistency this item sits on top of: main_resubmission.tex:218 quotes ICC 0.78-0.99 (original arm, from cluster_bootstrap_full.csv) as the explanation for corrected-arm deffs of 2.80/2.58, but inverting 1+(m*-1)rho with m*=4.45, deff=2.80 requires rho=0.522 (audit_recovered/FINAL_REAUDIT.md:177-186). Either quote the corrected-arm ICC range (~0.52-1.00) or state explicitly which arm the 0.78-0.99 belongs to. Note that check_numbers.py:528 currently ENFORCES the original-arm value, so that check must be updated in the same commit or it will fail.
- Close audit_recovered/decisions.jsonl D011 (status 'open') once the corrected-arm CRVE lands, and append a new decision recording the CR2 result.

**Notes:** Headline: the "keep" half of E4 is done, the "add" half is not. A Liang-Zeger CRVE genuinely ran and produced real numbers, so this is not NOT_DONE -- but it is CR0, not CR2/HC2; it is computed on the as-shipped arm rather than the corrected arm the item explicitly names; it is on an accuracy mean rather than on any delta; and not one of its numbers appears anywhere in main_resubmission.tex or supplementary.tex. The manuscript's only CRVE content is the unquantified clause at main_resubmission.tex:91.

Three things the main thread should know before executing:

(1) The item's premise is factually wrong for these data, and that is good news. Corrected-arm cluster counts are 5,833 and 23,360 (audit_recovered/INDEPENDENT_TRACKS.md:407,409), not modest. CR2 will be indistinguishable from CR0 at any reported precision. That makes this a cheap item to discharge -- but the answer must be computed and printed, because right now the manuscript says nothing at all, and "we didn't do it because we judged it wouldn't matter" is a much weaker reply than "we did it and here is the number."

(2) This item is entangled with an already-open ledger finding. D011 (decisions.jsonl:12, status "open") says the entire design-effect answer to R3.3 characterises the wrong arm, and names exp_clusterboot_full.py:48-49 as the source. E4 and D011 are the same defect surfacing twice -- the CRVE inherits the arm error from the ICC/deff computation directly above it. Fix them together; fixing E4 alone leaves the analysis it cross-checks still on the wrong arm.

(3) Two contradictions will bite whoever does the work. First, the corrected-arm numbers the manuscript now prints (4.45, 2.58, 2.80, 3.96x, 43.4%, 47.7%) have NO backing CSV -- zero hits repo-wide in results/*.csv. They live only in prose and in a working note. So the current design-effect paragraph is unreproducible from committed artifacts, and check_numbers cannot see it. Second, main_resubmission.tex:218 offers ICC 0.78-0.99 (original arm) as the explanation for corrected-arm deffs; inverting the paper's own formula gives rho=0.522, not 0.9071 (FINAL_REAUDIT.md:177-186). Worse, check_numbers.py:528 actively pins that 0.78-0.99 to the original-arm CSV column, so correcting the prose without touching the checker will break the gate. Sequence the edits accordingly.

Also missing and trivially fixable: paper/references.bib (37 entries) cites neither Liang-Zeger nor Kish, though both are named methods in the bundle.

## E5 — PARTIAL (heavy-compute)

**Remaining:**
- Write audit/experiments/exp_seed_factorial.py implementing the crossed 5x5 design for the two headline deltas (human_enhancers_ensembl and human_nontata_promoters, RF k=6). Reuse the frozen pipeline exactly as audit/pipeline/rf_seed_variance.py does (RA.featurize, H.kmer_binary_matrix k=8 + _edges at Jaccard 0.7 + connected_components, H._assign, make_models). Compute the near-duplicate components ONCE per dataset (they depend only on sequences), then loop split_seed in {0,1,2,3,4} x rf_seed in {0,1,2,3,4} = 25 cells per dataset, 50 fits total. Per cell record: split_seed, rf_seed, acc_orig, acc_corr, delta, and RF rank among the four models. Write results/seed_factorial.csv.
- In the same script, compute the variance decomposition on the 25-cell grid per dataset: a two-way random-effects ANOVA on the corrected accuracy (and on the delta) giving sigma^2_split, sigma^2_train, sigma^2_interaction, plus the residual, and report each as a percentage of total seed-induced variance. Write results/seed_variance_decomposition.csv with columns dataset, metric, var_split, var_train, var_interaction, var_total, pct_split, pct_train, pct_interaction. Note the interaction term is the component that is currently NOT estimable at all from the existing 5+5 marginal design — that is the scientific content the reviewer asked for.
- Recompute both headline combined-source Wald intervals using the FULL seed variance from the factorial rather than the split-seed variance alone. Current form at paper/main_resubmission.tex:91 is delta ± z*sqrt(s2_boot + s2_seed) where s2_seed is split-seed only (sd 0.0019 enhancers / 0.0055 nontata from results/step2_seed_variance.csv); the training-seed sd (0.00218 / 0.00348 from results/rf_seed_variance.csv) is currently reported but NEVER folded into either headline interval. The revised intervals must fold in the total seed variance from the decomposition. Expect the enhancers interval [15.4,17.4] and the nontata interval [8.8,12.9] to WIDEN — check whether either still excludes zero (both should, given deltas of 16.5 and 10.8 against seed sds of order 0.005, but verify rather than assume).
- Rewrite paper/main_resubmission.tex:172, replacing the sentence 'Corrected accuracy is stable across five re-split seeds and, independently, five forest training seeds ($0.698\pm0.002$; $0.821\pm0.003$), the forest demoted on every seed.' The word 'independently' concedes exactly what the reviewer objects to. Replace with a statement of the crossed 5x5 design, the variance decomposition percentages (split vs training vs interaction), and the statement that the forest is demoted in all 25 cells (verify this from the new CSV before writing it — do not assume it holds in every cell).
- Update paper/main_resubmission.tex:91 so the combined-source interval definition names the full variance: change 'fold the five re-split-seed variance into a combined-source interval' to reflect the 5x5 seed variance, and revise the sentence 'Models are not refit while the test set is resampled, so every interval is conditional on the fitted models.' — that limitation is now discharged for the two headline deltas specifically, and the sentence must be scoped to say the remaining intervals (the cluster bootstraps, the per-dose CIs) are still conditional while the two headline combined-source intervals are not.
- Propagate the revised headline intervals to paper/main_resubmission.tex:262 (the concluding paragraph currently carries 'combined-source $[15.4,17.4]$' and '$[8.8,12.9]$') and to paper/response_to_reviewers.md:542-543 and :630, which restate the combined-source construction and the 11.8-vs-10.8 estimand distinction. Check the abstract for the same numbers before finalising.
- Add a supplementary subsection to paper/supplementary.tex presenting the full 25-cell grid per dataset as a table plus the variance decomposition, and cite results/seed_factorial.csv and results/seed_variance_decomposition.csv by filename in the text (the supplement already uses this convention, e.g. line 464 cites results/exp_inject3class_deep.csv).
- Append an E5 decision line to audit_recovered/decisions.jsonl recording the factorial design, the decomposition result, and whether the widened intervals changed any verdict.
- Optional but cheap: audit/experiments/exp_dose_replication.py already specifies a 5 split x 3 model seed factorial (lines 16-20) for the inversion threshold and has never been run — its outputs results/dose_replication_trials.csv and results/dose_replication_summary.csv do not exist. Running it would let the same variance-decomposition machinery cover the dose threshold too, and would retire the single-realisation 0.106 threshold the script's own docstring criticises. Track separately from E5 if it is out of scope.

**Notes:** The single most load-bearing fact: main_resubmission.tex:172 says the two seed axes were varied 'independently' — that word is a literal concession that the design is 5+5=10 marginal runs, not the 5x5=25 crossed factorial the reviewer asked for. The code confirms it from both sides: rf_seed_variance.py's docstring says it reuses the seed-0 split and 'does NOT create a new partition', while step_variance_ci.py varies the split with a fixed model seed. So the interaction variance component is not estimable from anything currently in the repo. Do not let anyone argue this is DONE on the strength of the two marginal sds being reported.

Second material gap, easy to miss: the combined-source Wald interval folds in SPLIT-seed variance only. The training-seed sd (0.002 / 0.003) appears in the text purely as a standalone stability remark and is not propagated into either headline interval. So even the partial mitigation that exists does not actually put training variance into the headline numbers — the reviewer's complaint stands against the intervals as printed.

Numbers I verified rather than took on trust: I recomputed the training-seed means and sds from results/rf_seed_variance.csv (enhancers mean 0.69828 sd 0.00218; nontata mean 0.82126 sd 0.00348) and they do match the '0.698±0.002; 0.821±0.003' printed at main_resubmission.tex:172. Both seed CSVs contain genuine per-seed values, so both experiments really ran. No fabricated numbers found.

Skepticism flag on the one factorial that does exist: audit/experiments/exp_dose_replication.py describes a 5x3 factorial in detail and reads as finished work, but its two declared output CSVs are absent and there is no log — it never ran. It is also the wrong target (dose threshold, not headline deltas) and the wrong shape (5x3, not 5x5), so it cannot be repurposed as evidence for E5.

Not SUPERSEDED: the bundle still leads with both headline deltas and still prints the 'conditional on the fitted models' caveat at main_resubmission.tex:91, so the item is live. Not NOT_DONE either — the honest caveat, both marginal sweeps with real numbers, and the split-seed combined-source intervals are genuine partial credit.

Cost warning for the main thread: 50 RF fits at full scale on the leaky datasets, plus re-bootstrapping for the revised intervals. The k=8 Jaccard component computation should be done once per dataset and cached, not recomputed per cell — recomputing it 25 times is the obvious way to make this run for hours instead of minutes.

## F1 — PARTIAL (cheap-compute)

**Remaining:**
- FIRST, fix the live regression before adding anything: paper/supplementary.tex currently has \ref{tab:roster} at line 128 with no matching \label anywhere in the bundle. Restore the roster table environment into supplementary.tex (it was present in the 23:44 build — recover the exact caption text from paper/supplementary.aux:23, which preserves it verbatim, or from the main.tex:242 variant minus its '\S\ref{sec:m-graded}' and 'agreeing with the four-model comparison of \S4.3' clauses which the supplement version had already dropped). Place it in \subsection{Model roster: full specification} (supplementary.tex:71) so it lands as Table S2 and main_resubmission.tex:177's hard-coded 'Supplementary Table~S2' resolves correctly again.
- Extend audit/pipeline/emit_tables.py build_roster() (lines 156-183) to emit the drop CI. Add a column so the header becomes `Model & Gap$_{\text{bal}}$ & Gap$_{\text{raw}}$ & As-shipped & Corrected & Drop [95\% CI] & Rank`, rendering results/roster_rankings.csv's drop_ci string (already formatted as '[0.3254, 0.3453]') alongside the point estimate. Keep the ^{*} marker or drop it as redundant once explicit CIs are shown — but if you drop it, update the caption sentence '$^{*}$ marks intervals excluding zero'.
- Extend build_roster() to emit human_nontata_promoters as well as human_enhancers_ensembl (currently hard-coded D='human_enhancers_ensembl' at emit_tables.py:159). All nine rows for that dataset are already in results/roster_rankings.csv (ExtraTrees rank_orig 1, drop 0.1667 [0.1480,0.1868]; RF 0.1083 [0.0925,0.1259]; MLP 0.1101 [0.0942,0.1272]; kNN-1 0.0423 [0.0302,0.0545]; kNN-15 0.0420 [0.0284,0.0564]; LinearSVC 0.0793 [0.0618,0.0960]; HGB 0.0635 [0.0481,0.0800]; LR 0.0407 [0.0245,0.0569]; GaussianNB 0.0007 [-0.0178,0.0200]). Emit it as a second panel under a \midrule with a dataset sub-header, so the reviewer's 'nine-model roster, both splits' is satisfied for both leaky datasets and main_resubmission.tex:187's claim about the nontata scramble is finally backed by a table. NO NEW EXPERIMENTS NEEDED.
- Add a new generator build_decomposition() to audit/pipeline/emit_tables.py writing paper/tab_decomp.tex — the four-model decomposition (reviewer's Table 4). Four rows (RF, LinearSVC, LR, HGB) x four columns: As-shipped | Novel-only (same fit) | Corrected | Residual. Sources: as-shipped and corrected from results/exp_rankings.csv (dataset=human_enhancers_ensembl, metric=acc, split in {original,corrected}); novel-only from results/graded_performance.csv (dataset=human_enhancers_ensembl, split=original, sim_bin='[0,0.5)'); residual computed as corrected − novel-only. Values must come out as: RF 0.8596 / 0.7698 / 0.6957 / −0.0741; LinearSVC 0.7980 / 0.7820 / 0.7975 / +0.0155; LR 0.7809 / 0.7649 / 0.7806 / +0.0157; HGB 0.7636 / 0.7488 / 0.7608 / +0.0120. Register the file in the `targets` dict at emit_tables.py:189-192 and update the module docstring (lines 22-24) which currently names only two output files.
- Insert the decomposition table into paper/main_resubmission.tex as Table 4, immediately after the \paragraph{The four-model comparison...} block (after line 175), as a single-column \begin{table}[tbp] (NOT table* — see D050's float-footprint constraint at audit_recovered/decisions.jsonl:51; the four-column body is narrow enough). Caption must state that the novel-only column is the SAME fitted model evaluated on the as-shipped split's novel stratum with no re-splitting and no retraining, and that Residual = Corrected − Novel-only is the retraining penalty. Label it \label{tab:decomp}.
- Rewrite paper/main_resubmission.tex:175 to cite Table~\ref{tab:decomp} and delete the 8 numbers now duplicated in the table (0.860, 0.696, 0.798, 0.770, 0.782, −0.074, +0.012, +0.016), keeping only the narrative: that the best model changes, that the novel-stratum gap is 1.2 points, that the remaining ~10 points is a retraining penalty, and the AUROC/F1/bootstrap-probability robustness clause. Target roughly halving that line's 15 decimal literals.
- Rewrite paper/main_resubmission.tex:177 to cite the restored roster table and delete the numbers now duplicated there (0.8736, 0.8729, 0.8721, 0.0015, 0.768, 0.537, 0.628, and the rank strings 5→1, 6→2, 2→9). Keep the numbers the roster table does NOT carry: the top-two margin interval [-0.005,0.006], the swap margins [0.071,0.080] as-shipped and [0.024,0.035] corrected (results/roster_rankings.csv columns top2_margin_orig_ci, swap_margin_orig_ci, swap_margin_corr_ci), the 0.500 base rate and n=30,971. Consider promoting those three intervals into the roster caption instead.
- Fix the false sentence at paper/supplementary.tex:100: 'The nine-model roster itself is Table~3 of the main text' → point at the restored supplement table via \ref{tab:roster} (or at the main text only if you instead decide to promote the roster into main_resubmission.tex as Table 5). Main-text Table 3 is tab:manip.
- Decide and make consistent WHERE the roster lives, because the reviewer asked for it as main-text 'Table 5'. Option A (cheaper, respects the 8-page budget): keep it as Supplementary Table S2 and leave main_resubmission.tex:177's pointer — but then the response letter must say so explicitly rather than implying a main-text table. Option B (literal compliance): promote it into main_resubmission.tex as Table 5 and update supplementary.tex:100/:128 plus response_to_reviewers.md:84,658,673 to point at the main text. Check the page count after either choice: main_resubmission.tex:99 and :143 already use full-width table*, so a third full-width float is the real risk.
- Run `./venv/bin/python -m audit.pipeline.emit_tables` to regenerate all three .tex bodies, then `./venv/bin/python -m audit.pipeline.emit_tables --check` to confirm they are current, then rebuild via paper/build.sh and grep the fresh main_resubmission.log and supplementary.log for 'undefined' / 'Table ??' — the current logs are clean only because they predate the deletion. Note pdflatex is NOT installed on this machine (only /opt/homebrew/bin/tectonic), so the build environment needs checking first.
- Re-run audit/tools/check_numbers.py after the prose edits — D002 (decisions.jsonl:3) records the 120/120 invariant that must be preserved, and D059 (:60) lists 34 currently-open gate failures that this work will change the shape of.
- Update paper/response_to_reviewers.md to point at the two new tables in its reply to this presentation item, and fix the three 'Table S2' references at lines 84, 658 and 673 to match whichever placement Option A/B is chosen.

**Notes:** Headline: this item is PARTIAL and it is actively REGRESSING. Half of the requested Table 5 (the nine-model roster) genuinely existed and was typeset as Supplementary Table S2 in the 2026-07-19 23:44 build — pdftotext of paper/supplementary.pdf line 183 shows it. Someone then edited paper/supplementary.tex at 2026-07-20 01:04 and deleted the table environment. The generated body paper/tab_roster.tex survives but is now \input by NOTHING in the canonical bundle (only by the superseded paper/main.tex:243). This leaves three live defects the next build will surface: (1) a dangling \ref{tab:roster} at supplementary.tex:128 that will render 'Table ??'; (2) supplementary.tex:100 asserting the roster 'is Table~3 of the main text' when main-text Table 3 is the manipulations table; (3) main_resubmission.tex:177's hard-coded 'Supplementary Table~S2' now resolving to the Jaccard sensitivity table, with the same stale pointer repeated three times in response_to_reviewers.md. The current .log files show no undefined references ONLY because they predate the deletion — do not trust them.

Second: the fix here is pure authoring, not science. Every number both tables need is already committed and I verified it reproduces. In particular I confirmed the manuscript's decomposition residuals ('−0.074 against +0.012 to +0.016', main_resubmission.tex:175) are exactly corrected−novel-only computed from results/exp_rankings.csv and results/graded_performance.csv: RF −0.0741, LR +0.0157, LinearSVC +0.0155, HGB +0.0120. No experiment needs re-running for either table.

Third, a gap the reviewer's wording exposes: even the surviving tab_roster.tex does not meet the ask. It renders drops as '$0.105^{*}$' with an asterisk footnote, not as 'drops with CIs' — yet results/roster_rankings.csv carries a drop_ci column that emit_tables.py:180 reads past without using. And it covers only human_enhancers_ensembl, while main_resubmission.tex:187 makes a claim about the nine-model scramble on human_nontata_promoters whose supporting roster is in the CSV but in no table.

Fourth, a tension the main thread must resolve rather than paper over: the reviewer asked for main-text Tables 4 and 5, but decisions.jsonl:49 (D048) and :51 (D050) — both still status 'open' — are the standing instruction to CUT float footprint to reach 8 pages, and are almost certainly why the roster left the main text in the first place. Adding two tables directly contradicts an unresolved internal decision. The exemplar cited in D050 fits 10 floats into 8 pages and main_resubmission.tex currently has 3 tables + 4 figures, so it is probably feasible — but two of those tables (main_resubmission.tex:99, :143) are full-width table*, and the page count must be re-measured after the change, not assumed.

Minor: audit_recovered/STATE.md:168 uses the label 'F1' for an unrelated internal finding about manuscript defensiveness. Do not confuse it with this reviewer item — there is no ledger entry for this one at all.

## F3 — PARTIAL (text-only)

**Remaining:**
- paper/main_resubmission.tex:101 — fix the undercount in the Table 1 caption. Change "but three of these deltas---\textit{human\_ocr\_ensembl}, \textit{demo\_human\_or\_worm} and \textit{human\_enhancers\_cohn}---are computed on $20{,}000$-sequence subsamples" to "but four of these deltas---\textit{human\_ocr\_ensembl}, \textit{demo\_human\_or\_worm}, \textit{human\_enhancers\_cohn} and \textit{human\_ensembl\_regulatory}---are computed on $20{,}000$-sequence subsamples". Leave the following admissibility sentence and the \S\ref{sec:m-data} cross-reference exactly as they are; they are correct.
- paper/main_resubmission.tex:60 — make Methods agree. "All censuses and leak fractions are full-scale; four accuracy deltas are not" must become "five accuracy deltas are not" (four Table 1 rows + the clean-control roster arm), and add \textit{human\_ensembl\_regulatory} to the list "The corrected-split deltas for human_ocr_ensembl, demo_human_or_worm and human_enhancers_cohn".
- paper/main_resubmission.tex:60 — repair the bound sentence when adding the 3-class set. "Their full-scale leak fractions are $\le0.012$ (Jaccard) and $\le0.068$ (containment)" survives on the Jaccard side (human_ensembl_regulatory is 0.0049, from results/leakage_full.csv:10), but NO containment value exists for the 3-class set — paper/tab_reportcard.tex:15 prints "--" in that column. Reword to something like "$\le0.012$ (Jaccard), and $\le0.068$ (containment) on the binary sets, containment being undefined for the three-class row". Do not invent a containment number for it.
- Do NOT rerun anything. No experiment is missing: results/all_results_long.csv already records n_used=20000/subsampled=True for all four rows and results/leakage_full.csv already records the full-scale census. This is a text-only disclosure fix.
- Optional consistency sweep (same caveat, different float): paper/main_resubmission.tex:182, the Fig. 3 caption, says the clean control demo_human_or_worm is a "$20$k subsample" but does not repeat the admissibility clause. If the reviewer's intent is read broadly as 'everywhere subsampled numbers appear', append a short clause there, e.g. "(admissible to confirm cleanliness, not to establish it; \S\ref{sec:m-data})".
- Do NOT touch paper/main.tex:191. That is the superseded 20-page caption; leaving it stale is correct given main_resubmission.tex is the canonical bundle.

**Notes:** The literal presentation ask is satisfied — the subsample-admissibility caveat IS repeated in the Table 1 caption at paper/main_resubmission.tex:101, is well-phrased ('admissible to confirm cleanliness and never to establish it'), and cross-references \\S\\ref{sec:m-data}. I am calling this PARTIAL rather than DONE because the caveat as written does not cover all the places the subsampled deltas actually appear in that table: it names three rows, but four of Table 1's eight rows carry a subsampled accuracy delta. The omitted row is human_ensembl_regulatory (3-class), whose printed Acc. lost of -0.010 I reproduced exactly from 20,000-sequence-subsample runs (0.6639118 original minus 0.6737606 mean-of-three-seeds corrected = -0.00985). So the caption currently asserts something false about its own table. Two mitigating facts the main thread should weigh: (1) the substance of the caveat is unharmed — the omitted row's full-scale Jaccard leak is 0.0049 (results/leakage_full.csv:10), so a subsample there is admissible under the paper's own rule, and no result changes; (2) the same undercount is inherited from Methods at main_resubmission.tex:60 ('four accuracy deltas are not'), so fixing only the caption would leave caption and Methods disagreeing — both must move together. Watch the containment bound at :60: the three-class row has no containment value (tab_reportcard.tex:15 prints '--'), so the existing '$\\le0.068$ (containment)' claim cannot simply absorb the new dataset. Finally, there is no ledger trace for this item; the 'F3' appearing in audit_recovered/decisions.jsonl D024 and MAIN_THREAD_FINDINGS.md:46 is an unrelated finding about abstract length, so do not treat that as prior sign-off on this one.
